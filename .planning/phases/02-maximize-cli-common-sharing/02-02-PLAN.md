---
phase: 02-maximize-cli-common-sharing
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - crates/gator/src/main.rs
  - crates/gator/src/cli.rs
  - crates/todoer/src/main.rs
  - crates/todoer/src/cli.rs
  - crates/silent-critic/src/main.rs
  - crates/silent-critic/src/cli.rs
  - crates/bsky-comment-extractor/src/main.rs
  - crates/bsky-comment-extractor/src/cli.rs
autonomous: true
requirements:
  - CLI-SHARE-01
  - CLI-SHARE-03
must_haves:
  truths:
    - "adapter-heavy tools stop defining local metadata boilerplate that `cli-common` can provide"
    - "`gator`, `todoer`, `silent-critic`, and `bce` preserve their existing invocation syntax while deleting local `tool_spec()` / `NoDoctor` / `run_meta_command()` glue where possible"
    - "shared command-response helpers reduce repeated JSON-vs-text success handling where practical"
  artifacts:
    - path: "crates/gator/src/main.rs"
      provides: "gator entrypoint using shared metadata helpers"
      contains: "tftio_cli_common"
    - path: "crates/todoer/src/main.rs"
      provides: "todoer command flow with reduced local boilerplate"
      contains: "tftio_cli_common"
    - path: "crates/silent-critic/src/main.rs"
      provides: "silent-critic command flow with reduced local boilerplate"
      contains: "tftio_cli_common"
    - path: "crates/bsky-comment-extractor/src/main.rs"
      provides: "bce metadata-command flow using shared adapters"
      contains: "tftio_cli_common"
  key_links:
    - from: "crates/gator/src/main.rs"
      to: "crates/cli-common/src/app.rs"
      via: "shared workspace tool metadata"
      pattern: "workspace_tool|ToolSpec"
    - from: "crates/todoer/src/main.rs"
      to: "crates/cli-common/src/command.rs"
      via: "shared metadata-command helpers"
      pattern: "run_standard_command|meta"
---

<objective>
Migrate the adapter-heavy tools onto the expanded `cli-common` surface so they delete the local boilerplate that still wrapped Phase 01 primitives.

Purpose: prove the new shared abstractions work on multiple command-tree shapes without breaking existing behavior.
Output: `gator`, `todoer`, `silent-critic`, and `bce` simplified to domain logic plus the shared CLI infrastructure.
</objective>

<execution_context>
@/Users/jfb/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jfb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-maximize-cli-common-sharing/02-RESEARCH.md
@.planning/phases/02-maximize-cli-common-sharing/02-01-PLAN.md

<interfaces>
Low-risk targets with obvious removable boilerplate:
- `gator`
- `todoer`
- `silent-critic`
- `bsky-comment-extractor`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove repeated metadata/doctorless/meta-command adapters from the low-risk tools</name>
  <files>crates/gator/src/main.rs, crates/todoer/src/main.rs, crates/silent-critic/src/main.rs, crates/bsky-comment-extractor/src/main.rs</files>
  <read_first>
    - crates/gator/src/main.rs
    - crates/todoer/src/main.rs
    - crates/silent-critic/src/main.rs
    - crates/bsky-comment-extractor/src/main.rs
    - crates/cli-common/src/app.rs
    - crates/cli-common/src/command.rs
    - crates/cli-common/src/doctor.rs
  </read_first>
  <action>
Use the new `cli-common` adapters to delete as much local entrypoint glue as possible:

1. Replace local `tool_spec()` definitions with shared constructors/presets where the resulting metadata is still explicit and readable.
2. Replace local `NoDoctor` structs with the shared doctorless adapter.
3. Replace local `run_meta_command()` mappers with the shared metadata-command helper APIs wherever the clap tree allows it.
4. Keep per-tool command syntax stable.
  </action>
  <verify>
    <automated>cargo test -p tftio-gator -p tftio-todoer -p tftio-silent-critic -p tftio-bsky-comment-extractor && cargo clippy -p tftio-gator -p tftio-todoer -p tftio-silent-critic -p tftio-bsky-comment-extractor -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - these crates no longer define removable local metadata boilerplate that `cli-common` now provides
    - all four crates preserve their current command syntax and pass their targeted tests
    - targeted `cargo clippy` exits 0
  </acceptance_criteria>
  <done>
    The lower-risk tools now consume the richer `cli-common` surface instead of maintaining local wrappers around it.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract any generally useful response-handling helpers revealed by these migrations</name>
  <files>crates/cli-common/src/json.rs, crates/cli-common/src/error.rs, crates/todoer/src/main.rs, crates/silent-critic/src/main.rs</files>
  <read_first>
    - crates/todoer/src/main.rs
    - crates/silent-critic/src/main.rs
    - crates/cli-common/src/json.rs
    - crates/cli-common/src/error.rs
  </read_first>
  <action>
As the low-risk migrations land, identify any command-response boilerplate that still appears in at least two crates and move it into `cli-common` if it is genuinely reusable.

Focus on:
- JSON-vs-text response helpers
- shared success/failure dispatch helpers
- any small wrapper APIs that materially reduce repetition without obscuring command behavior

Do not move domain-specific tables, summaries, or business logic.
  </action>
  <verify>
    <automated>cargo test -p tftio-gator -p tftio-todoer -p tftio-silent-critic -p tftio-bsky-comment-extractor && just cli-metadata-consistency</automated>
  </verify>
  <acceptance_criteria>
    - any newly extracted response helper is used by at least two tools or is clearly broadly useful
    - repository shell CLI consistency tests remain green
    - no domain-specific rendering moved into `cli-common`
  </acceptance_criteria>
  <done>
    The first migration wave leaves behind only domain output and business logic in these tools.
  </done>
</task>
