---
phase: 02-maximize-cli-common-sharing
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - crates/unvenv/src/main.rs
  - crates/asana-cli/src/cli/mod.rs
  - crates/asana-cli/src/doctor.rs
  - crates/asana-cli/src/main.rs
  - crates/prompter/src/main.rs
  - crates/prompter/src/completions.rs
  - crates/prompter/src/doctor.rs
  - crates/prompter/src/lib.rs
autonomous: true
requirements:
  - CLI-SHARE-02
  - CLI-SHARE-03
must_haves:
  truths:
    - "`unvenv`, `asana-cli`, and `prompter` consume the expanded `cli-common` surface for generally useful doctor, completion, and output behavior"
    - "`prompter` keeps its dynamic completion augmentation and custom doctor state source while deleting duplicate rendering logic"
    - "`asana-cli` and `unvenv` keep domain-specific behavior local while removing now-shared utility code"
  artifacts:
    - path: "crates/prompter/src/completions.rs"
      provides: "only augmentation logic on top of shared completion rendering"
      contains: "augment_"
    - path: "crates/prompter/src/doctor.rs"
      provides: "shared doctor-report rendering over prompter-specific state collection"
      contains: "DoctorReport|collect_doctor_state"
    - path: "crates/unvenv/src/main.rs"
      provides: "reduced local output/metadata boilerplate"
      contains: "tftio_cli_common"
    - path: "crates/asana-cli/src/cli/mod.rs"
      provides: "shared doctor/output helpers where generally useful"
      contains: "tftio_cli_common"
  key_links:
    - from: "crates/prompter/src/completions.rs"
      to: "crates/cli-common/src/completions.rs"
      via: "shared render-first completion wrapper"
      pattern: "render_.*completion|generate_completions"
    - from: "crates/prompter/src/doctor.rs"
      to: "crates/cli-common/src/doctor.rs"
      via: "shared report rendering"
      pattern: "DoctorReport|run_doctor"
---

<objective>
Migrate the richer consumer crates onto the expanded `cli-common` APIs and extract the final generally useful completion/doctor/output helpers.

Purpose: finish the maximal-sharing effort on the tools that still had legitimate reasons to keep local wrappers during Phase 01.
Output: `unvenv`, `asana-cli`, and `prompter` keep only domain-specific behavior while leaning on `cli-common` for the rest.
</objective>

<execution_context>
@/Users/jfb/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jfb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-maximize-cli-common-sharing/02-RESEARCH.md
@.planning/phases/02-maximize-cli-common-sharing/02-01-PLAN.md
@.planning/phases/02-maximize-cli-common-sharing/02-02-PLAN.md

<interfaces>
Higher-complexity consumers with remaining shared-behavior duplication:
- `unvenv`
- `asana-cli`
- `prompter`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move shared completion and doctor rendering out of the richer tools</name>
  <files>crates/prompter/src/completions.rs, crates/prompter/src/doctor.rs, crates/asana-cli/src/doctor.rs, crates/cli-common/src/completions.rs, crates/cli-common/src/doctor.rs</files>
  <read_first>
    - crates/prompter/src/completions.rs
    - crates/prompter/src/doctor.rs
    - crates/asana-cli/src/doctor.rs
    - crates/cli-common/src/completions.rs
    - crates/cli-common/src/doctor.rs
  </read_first>
  <action>
Use the shared buffer/report primitives from Plan 01 to move duplicated rendering logic into `cli-common`:

1. Refactor `prompter` so local completion code only augments a shared rendered script.
2. Refactor `prompter` doctor JSON/plain rendering so local code only collects state and shared code renders it.
3. Move any doctor helper logic from `asana-cli` or similar crates into `cli-common` if it is broadly reusable.
4. Preserve all crate-specific data sources and command flows.
  </action>
  <verify>
    <automated>cargo test -p tftio-prompter -p tftio-asana-cli && cargo clippy -p tftio-prompter -p tftio-asana-cli -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `prompter` no longer duplicates generic completion instruction/script rendering
    - `prompter` no longer duplicates generic doctor report rendering
    - targeted tests for `prompter` and `asana-cli` remain green
  </acceptance_criteria>
  <done>
    The richest remaining shared UX renderers now live in `cli-common`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reduce remaining local output and metadata boilerplate in `unvenv`, `asana-cli`, and `prompter`</name>
  <files>crates/unvenv/src/main.rs, crates/asana-cli/src/cli/mod.rs, crates/asana-cli/src/main.rs, crates/prompter/src/main.rs, crates/prompter/src/lib.rs</files>
  <read_first>
    - crates/unvenv/src/main.rs
    - crates/asana-cli/src/cli/mod.rs
    - crates/asana-cli/src/main.rs
    - crates/prompter/src/main.rs
    - crates/prompter/src/lib.rs
    - crates/cli-common/src/app.rs
    - crates/cli-common/src/output.rs
    - crates/cli-common/src/error.rs
  </read_first>
  <action>
Delete any remaining local boilerplate that the expanded shared surface can now absorb:

1. Replace reusable tool metadata/output helpers with `cli-common` equivalents.
2. Preserve `unvenv` scan behavior, `asana-cli` command tree/manpage behavior, and `prompter` app-mode/domain behavior.
3. If some duplicated code must remain local, document in comments or the summary why it is intentionally crate-specific.
  </action>
  <verify>
    <automated>cargo test -p tftio-unvenv -p tftio-asana-cli -p tftio-prompter && cargo clippy -p tftio-unvenv -p tftio-asana-cli -p tftio-prompter -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - shared boilerplate is reduced further across these three crates
    - domain-specific behavior remains intact
    - targeted tests and clippy checks exit 0
  </acceptance_criteria>
  <done>
    The richer consumer crates now keep only genuinely local behavior on top of the shared CLI infrastructure.
  </done>
</task>
