---
phase: 07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools
plan: 06
type: execute
wave: 4
depends_on: [07-03, 07-04, 07-05]
files_modified:
  - crates/prompter/src/lib.rs
  - crates/prompter/src/main.rs
  - crates/prompter/tests/agent_surface.rs
  - justfile
  - tests/cli/lib.sh
  - tests/cli/06-agent-mode.sh
autonomous: true
requirements: [D-04, D-05, D-06, D-07, D-08, D-09, D-13]

must_haves:
  truths:
    - "`prompter` respects the shared filtered surface even though it uses a custom `AppMode` parser."
    - "Workspace CLI consistency checks run from the in-repo metadata script instead of an external path."
    - "A single repo-level smoke test proves agent help, one allowed `--agent-skill` path, and hidden-command rejection across every migrated `cli-common` consumer."
    - "The rollout wave includes at least one repo-level smoke assertion that ordinary agent-mode `--help` or `help` output stays redacted and one assertion that typo/suggestion paths stay redacted."
    - "The phase does not pass rollout until the full Cargo workspace test gate succeeds after all consumer migrations and CLI consistency updates."
  artifacts:
    - path: "crates/prompter/src/lib.rs"
      provides: "Factored AppMode resolution that can reuse shared clap parsing"
      contains: "resolve_app_mode"
    - path: "justfile"
      provides: "In-repo `cli-metadata-consistency` target"
      contains: "./scripts/test-cli-metadata-consistency.sh"
    - path: "tests/cli/06-agent-mode.sh"
      provides: "Workspace-wide agent-surface smoke coverage"
      contains: "TFTIO_AGENT_TOKEN"
  key_links:
    - from: "crates/prompter/src/main.rs"
      to: "crates/prompter/src/lib.rs"
      via: "shared clap parse path feeds factored AppMode resolution"
      pattern: "resolve_app_mode|parse_with_agent_surface"
    - from: "tests/cli/06-agent-mode.sh"
      to: "tests/cli/lib.sh"
      via: "shared env-var helper activates agent mode for every package smoke test"
      pattern: "run_agent"
    - from: "tests/cli/06-agent-mode.sh"
      to: "07-VALIDATION.md"
      via: "rollout smoke and summary must cover both repo-level redaction checks and the manual inspectability review handoff"
      pattern: "inspect `--agent-help` and at least one `--agent-skill <name>` output"
    - from: "07-VALIDATION.md"
      to: "cargo test --workspace --verbose"
      via: "final rollout gate after Plan 07-06 finishes"
      pattern: "cargo test --workspace --verbose"
---

<objective>
Finish the Phase 7 rollout by adapting `prompter`'s custom parser and restoring workspace-level validation for every `cli-common` consumer.

Purpose: `prompter` is the only non-standard parser shape in the workspace, and the phase is incomplete until the repo-level smoke harness exercises all seven migrated tools through the shared agent contract.
Output: Shared agent-mode adoption for `prompter`, repaired `just cli-consistency`, and a workspace-wide agent-mode smoke script.
</objective>

<execution_context>
@/Users/jfb/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jfb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-CONTEXT.md
@.planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-RESEARCH.md
@crates/prompter/src/lib.rs
@crates/prompter/src/main.rs
@justfile
@tests/cli/lib.sh
@scripts/test-cli-metadata-consistency.sh

<interfaces>
Current prompter parse split:
```rust
pub fn parse_args_from(args: Vec<String>) -> Result<AppMode, String>;
pub enum AppMode { Help, Version { json: bool }, ... }
```

Phase 7 goal for prompter:
```rust
pub fn resolve_app_mode(cli: Cli) -> Result<AppMode, String>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adapt `prompter`'s custom `AppMode` parser to the shared agent surface</name>
  <read_first>
    - crates/prompter/src/lib.rs
    - crates/prompter/src/main.rs
    - crates/cli-common/src/agent.rs
    - .planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-RESEARCH.md
  </read_first>
  <files>crates/prompter/src/lib.rs, crates/prompter/src/main.rs, crates/prompter/tests/agent_surface.rs</files>
  <behavior>
    - Test 1: In agent mode, `prompter --agent-help` lists only `render-prompts`, `list-profiles`, `tree-profiles`, and `validate-profiles` (D-07, D-09).
    - Test 2: In agent mode, `prompter --agent-skill render-prompts` prints only the render contract (D-08).
    - Test 3: In agent mode, hidden commands such as `doctor`, `license`, `version`, `completions`, and `init` parse as nonexistent (D-04, D-05, D-06).
  </behavior>
  <action>
1. In `crates/prompter/src/lib.rs`, factor the `match cli.command { ... }` logic out of `parse_args_from(...)` into a new function named exactly `resolve_app_mode(cli: Cli) -> Result<AppMode, String>`.

2. In `crates/prompter/src/main.rs`, replace `parse_args()` with a new startup path that:
   - uses the shared `parse_with_agent_surface_from::<Cli>(...)` helper from Plan 07-02 on raw argv
   - converts the accepted `Cli` into `AppMode` via `resolve_app_mode(cli)`
   - prints shared `--agent-help` / `--agent-skill` output when requested

3. Declare these exact capability slugs for agent mode:
   - `render-prompts` → `run`
   - `list-profiles` → `list`
   - `tree-profiles` → `tree`
   - `validate-profiles` → `validate`
   Rely on the shared default-generation strategy from Plan 07-02 for any capability prose fields not explicitly customized.
   Do not declare `version`, `license`, `completions`, `doctor`, or `init`.

4. Add `crates/prompter/tests/agent_surface.rs` covering visible help, visible skill output, and hidden `doctor` rejection under the shared env vars.
  </action>
  <acceptance_criteria>
    - `crates/prompter/src/lib.rs` contains `fn resolve_app_mode`
    - `crates/prompter/src/main.rs` contains `parse_with_agent_surface_from`
    - `crates/prompter/src/main.rs` or `crates/prompter/src/lib.rs` contains the four capability slugs listed above
    - `crates/prompter/tests/agent_surface.rs` exists
    - `cargo test -p tftio-prompter agent_surface` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p tftio-prompter agent_surface</automated>
  </verify>
  <done>`prompter` respects the shared filtered surface despite its custom AppMode architecture.</done>
</task>

<task type="auto">
  <name>Task 2: Restore workspace-level CLI consistency checks with a shared agent-mode smoke script</name>
  <read_first>
    - justfile
    - tests/cli/lib.sh
    - tests/cli/01-metadata-commands.sh
    - scripts/test-cli-metadata-consistency.sh
    - .planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-VALIDATION.md
  </read_first>
  <files>justfile, tests/cli/lib.sh, tests/cli/06-agent-mode.sh</files>
  <action>
1. In `justfile`, change the `cli-metadata-consistency` recipe to call the in-repo script exactly as `./scripts/test-cli-metadata-consistency.sh` instead of the missing external path.

2. In the same `justfile`, update the `cli-consistency` recipe so it explicitly executes `tests/cli/06-agent-mode.sh` after the metadata consistency and package help checks. The new repo-level smoke script must be part of the rollout gate, not an orphaned artifact.

3. In `tests/cli/lib.sh`, add a helper named exactly `run_agent()` that exports:
   - `TFTIO_AGENT_TOKEN=phase7-test-token`
   - `TFTIO_AGENT_TOKEN_EXPECTED=phase7-test-token`
   and then runs `cargo run -q -p "$package" -- "$@"`.

4. Create `tests/cli/06-agent-mode.sh` that proves the shared agent surface across every migrated consumer:
   - `bsky-comment-extractor` → `--agent-help`, `--agent-skill query-posts`, hidden `fetch`
   - `gator` → `--agent-help`, `--agent-skill run-agent`, hidden `meta version`
   - `todoer` → `--agent-help`, `--agent-skill list-tasks`, hidden `meta version`
   - `unvenv` → `--agent-help`, `--agent-skill scan-venvs`, hidden `doctor`
   - `asana-cli` → `--agent-help`, `--agent-skill manage-tasks`, hidden `doctor`
   - `silent-critic` → `--agent-help`, `--agent-skill session-manifest`, hidden `project init`
   - `prompter` → `--agent-help`, `--agent-skill render-prompts`, hidden `doctor`
   Use the shared `run_agent()` helper plus existing `assert_contains` / `assert_not_contains` helpers.

5. In the same repo-level smoke script, add at least one explicit redaction assertion for each of these shared-surface failure modes:
   - ordinary agent-mode `--help` or `help` output remains redacted (for example, `silent-critic --help` or `silent-critic help` must omit hidden higher-role commands)
   - a typo/suggestion-path invocation stays redacted (for example, typo a hidden command or flag under agent mode and assert the stderr text omits both the hidden command name and any `Did you mean` suggestion)
   These repo-level assertions are the rollout-wave counterpart to the stronger `cli-common` substrate tests from Plan 07-02.

6. Do not introduce sleeps or watch-mode loops. The script must remain a deterministic shell smoke test that can be called by `just cli-consistency`.
  </action>
  <acceptance_criteria>
    - `justfile` contains `./scripts/test-cli-metadata-consistency.sh`
    - `justfile` contains a `cli-consistency` recipe step that executes `tests/cli/06-agent-mode.sh`
    - `tests/cli/lib.sh` contains `run_agent()`
    - `tests/cli/lib.sh` contains both `TFTIO_AGENT_TOKEN=phase7-test-token` and `TFTIO_AGENT_TOKEN_EXPECTED=phase7-test-token`
    - `tests/cli/06-agent-mode.sh` exists
    - `tests/cli/06-agent-mode.sh` covers all seven packages listed above and exercises one allowed `--agent-skill <name>` path for each of them
    - `tests/cli/06-agent-mode.sh` includes at least one repo-level assertion for ordinary agent-mode `--help`/`help` redaction
    - `tests/cli/06-agent-mode.sh` includes at least one repo-level assertion that typo/suggestion-path output omits hidden names and `Did you mean`
    - `just cli-consistency` exits 0
  </acceptance_criteria>
  <verify>
    <automated>just cli-consistency</automated>
  </verify>
  <done>The repo-level CLI consistency suite runs entirely in-repo and proves the shared agent-mode contract across every `cli-common` consumer.</done>
</task>

<task type="auto">
  <name>Task 3: Run the final workspace-wide rollout gate required by Phase 7 validation</name>
  <read_first>
    - .planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-VALIDATION.md
    - justfile
    - tests/cli/06-agent-mode.sh
  </read_first>
  <files>justfile, tests/cli/06-agent-mode.sh, Cargo.toml</files>
  <action>
1. After Task 1 and Task 2 are complete, carry forward the manual inspectability review from `07-VALIDATION.md` into the rollout summary by recording concrete commands for operator review and, if execution mode allows, performing a spot check on at least:
   - one migrated tool's `--agent-help` output
   - one migrated tool's `--agent-skill <name>` output
   The summary must state whether this manual review was performed during execution or remains pending for operator sign-off.

2. Run the final workspace gate required by `07-VALIDATION.md` exactly as `cargo test --workspace --verbose`.

3. Treat this as the Phase 7 rollout blocker, not an optional extra smoke check. If the command fails, fix only the fallout exposed by the completed Phase 7 migrations or the shared CLI consistency wiring from this plan before rerunning.

4. Do not claim the phase rollout is complete until:
   - the repo-level smoke in Task 2 is green
   - the rollout summary records the manual inspectability review status from `07-VALIDATION.md`
   - `cargo test --workspace --verbose` exits 0
  </action>
  <acceptance_criteria>
    - `just cli-consistency` exits 0 before the final workspace gate is run
    - the execution summary for this plan records the `07-VALIDATION.md` manual inspectability review status and concrete review commands
    - `cargo test --workspace --verbose` exits 0
    - the execution summary for this plan reports the workspace-wide Cargo test gate as the final verification step required by `07-VALIDATION.md`
  </acceptance_criteria>
  <verify>
    <automated>bash tests/cli/06-agent-mode.sh</automated>
  </verify>
  <done>Phase 7 ends with the workspace-wide Cargo test gate green, matching the rollout requirement in `07-VALIDATION.md`.</done>
</task>

</tasks>

<verification>
- `cargo test -p tftio-prompter agent_surface`
- `just cli-consistency`
- `cargo test --workspace --verbose`
- `rg -n "run_agent|TFTIO_AGENT_TOKEN|query-posts|run-agent|list-tasks|scan-venvs|manage-tasks|session-manifest|render-prompts|project init" tests/cli`
</verification>

<success_criteria>
- `prompter` uses the shared agent-mode parser/help pipeline
- `just cli-consistency` no longer references a missing external script
- one workspace smoke script verifies `--agent-help`, one allowed `--agent-skill` path, hidden-command rejection across all migrated consumers, plus at least one normal-help redaction check and one typo/suggestion-path suppression check
- the rollout summary explicitly covers the manual inspectability review described in `07-VALIDATION.md`
- the full workspace test suite passes after the Phase 7 rollout changes
</success_criteria>

<output>
After completion, create `.planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-06-SUMMARY.md`
</output>
