---
phase: 07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cli-common/src/agent.rs
  - crates/cli-common/src/app.rs
  - crates/cli-common/src/lib.rs
autonomous: true
requirements: [D-01, D-02, D-03, D-10, D-11, D-12]

must_haves:
  truths:
    - "Agent mode becomes active only when the shared presented token exactly matches the shared expected token."
    - "Workspace tools can declare named agent capabilities in `cli-common` instead of ad hoc tool-local policy logic."
    - "The shared policy model can describe allowed commands, subcommands, and flags at the capability level."
  artifacts:
    - path: "crates/cli-common/src/agent.rs"
      provides: "Shared agent-mode env-var contract, capability types, and filtering helpers"
      contains: "TFTIO_AGENT_TOKEN"
    - path: "crates/cli-common/src/app.rs"
      provides: "ToolSpec support for an optional declarative agent surface"
      contains: "agent_surface"
    - path: "crates/cli-common/src/lib.rs"
      provides: "Public re-exports for the shared agent-mode API"
      contains: "pub mod agent;"
  key_links:
    - from: "crates/cli-common/src/app.rs"
      to: "crates/cli-common/src/agent.rs"
      via: "ToolSpec stores AgentSurfaceSpec for later parsing/rendering"
      pattern: "agent_surface"
    - from: "crates/cli-common/src/agent.rs"
      to: "std::env"
      via: "exact token equality check at process start"
      pattern: "TFTIO_AGENT_TOKEN_EXPECTED"
---

<objective>
Create the shared `cli-common` contract for token-gated agent mode before any workspace tool adopts it.

Purpose: Phase 7 depends on one source of truth for activation and capability declaration. If the env-var gate or policy model stays tool-local, D-01 through D-03 and D-10 through D-12 fail immediately.
Output: `cli-common` exports env-var constants, activation/context helpers, capability declaration types, and `ToolSpec` support for declarative agent surfaces.
</objective>

<execution_context>
@/Users/jfb/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jfb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-CONTEXT.md
@.planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-RESEARCH.md
@docs/the_silent_critic_tooling_design.md
@docs/the-silent-critic-system-spec.md
@docs/the_silent_critic_formal_appendix.md
@crates/cli-common/src/app.rs
@crates/cli-common/src/lib.rs

<interfaces>
From crates/cli-common/src/app.rs:
```rust
pub struct ToolSpec {
    pub bin_name: &'static str,
    pub display_name: &'static str,
    pub version: &'static str,
    pub license: LicenseType,
    pub repo: RepoInfo,
    pub supports_json: bool,
    pub supports_doctor: bool,
    pub supports_update: bool,
}
```

New contract this plan must create for downstream plans:
```rust
pub const AGENT_TOKEN_ENV: &str = "TFTIO_AGENT_TOKEN";
pub const AGENT_TOKEN_EXPECTED_ENV: &str = "TFTIO_AGENT_TOKEN_EXPECTED";

pub struct AgentModeContext { pub active: bool, ... }
pub struct AgentSurfaceSpec { pub capabilities: &'static [AgentCapability] }
pub struct AgentCapability { pub name: &'static str, ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define shared agent-mode types and the workspace token contract</name>
  <read_first>
    - .planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-CONTEXT.md
    - .planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-RESEARCH.md
    - docs/the_silent_critic_tooling_design.md
    - docs/the-silent-critic-system-spec.md
    - docs/the_silent_critic_formal_appendix.md
    - crates/cli-common/src/app.rs
    - crates/cli-common/src/lib.rs
  </read_first>
  <files>crates/cli-common/src/agent.rs, crates/cli-common/src/app.rs, crates/cli-common/src/lib.rs</files>
  <behavior>
    - Test 1: `AgentModeContext::detect()` returns inactive when either `TFTIO_AGENT_TOKEN` or `TFTIO_AGENT_TOKEN_EXPECTED` is absent (D-01, D-03).
    - Test 2: `AgentModeContext::detect()` returns active only when both env vars exist and their values are exactly equal (D-01, D-02, D-03).
    - Test 3: `ToolSpec` can carry an `AgentSurfaceSpec` with named `AgentCapability` entries without any tool-local procedural policy code (D-10, D-11, D-12).
  </behavior>
  <action>
1. Create `crates/cli-common/src/agent.rs` and define the shared env-var names exactly once:
   - `pub const AGENT_TOKEN_ENV: &str = "TFTIO_AGENT_TOKEN";`
   - `pub const AGENT_TOKEN_EXPECTED_ENV: &str = "TFTIO_AGENT_TOKEN_EXPECTED";`
   These concrete names satisfy D-01 and D-02.

2. In `agent.rs`, define the declarative policy types with const-friendly constructors so every tool can declare static capability metadata:
   - `AgentModeContext`
   - `AgentSurfaceSpec`
   - `AgentCapability`
   - `CommandSelector`
   - `FlagSelector`
   - optional supporting enums/structs for command-path matching and output/constraint text

3. Extend `ToolSpec` in `crates/cli-common/src/app.rs` with `pub agent_surface: Option<&'static AgentSurfaceSpec>` and add a const builder `with_agent_surface(...) -> Self`. Keep all existing workspace-tool constructors working for tools that have not adopted Phase 7 yet.

4. Re-export the new module and public types from `crates/cli-common/src/lib.rs` so downstream crates can import them from `tftio_cli_common`.

5. Write unit tests in `agent.rs` that cover:
   - missing presented token → inactive
   - missing expected token → inactive
   - exact string mismatch → inactive
   - exact string match → active
   - capability declarations preserve command paths and flag selectors
  </action>
  <acceptance_criteria>
    - `crates/cli-common/src/agent.rs` contains `pub const AGENT_TOKEN_ENV: &str = "TFTIO_AGENT_TOKEN"`
    - `crates/cli-common/src/agent.rs` contains `pub const AGENT_TOKEN_EXPECTED_ENV: &str = "TFTIO_AGENT_TOKEN_EXPECTED"`
    - `crates/cli-common/src/app.rs` contains `pub agent_surface: Option<&'static AgentSurfaceSpec>`
    - `crates/cli-common/src/app.rs` contains `with_agent_surface`
    - `crates/cli-common/src/lib.rs` re-exports `AgentModeContext`, `AgentSurfaceSpec`, and `AgentCapability`
    - `cargo test -p tftio-cli-common agent_mode_activation --lib` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p tftio-cli-common agent_mode_activation --lib</automated>
  </verify>
  <done>`cli-common` exposes the shared token gate and declarative capability types needed by every later Phase 7 plan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement capability-first filtering helpers in `cli-common`</name>
  <read_first>
    - crates/cli-common/src/agent.rs
    - crates/cli-common/src/app.rs
    - docs/the_silent_critic_tooling_design.md
    - docs/the_silent_critic_formal_appendix.md
  </read_first>
  <files>crates/cli-common/src/agent.rs</files>
  <behavior>
    - Test 1: A capability allowlist can keep one subcommand path visible while removing an undeclared sibling path (D-10, D-11).
    - Test 2: A capability allowlist can keep only the declared long flags for a command path while removing undeclared long flags in agent mode (D-04, D-10, D-11).
    - Test 3: When no capabilities are declared, the filtered surface is empty except for the shared agent-inspection flags (D-03, D-12).
  </behavior>
  <action>
1. In `crates/cli-common/src/agent.rs`, add helper functions that later plans will reuse without renaming:
   - `AgentModeContext::detect()`
   - `visible_capabilities(spec: &ToolSpec, ctx: &AgentModeContext) -> &[AgentCapability]`
   - `apply_agent_surface(command: &mut clap::Command, spec: &ToolSpec, ctx: &AgentModeContext)`

2. `apply_agent_surface(...)` must prune the actual `clap::Command` tree rather than only hiding help text. It should:
   - keep the human command tree untouched when `ctx.active == false`
   - remove undeclared subcommands, undeclared arguments, undeclared aliases, and undeclared completions-facing nodes when `ctx.active == true`
   - keep only the command paths and flags declared by allowed capabilities
   - preserve shared `--agent-help` and `--agent-skill` hooks for later plans

3. Use exact capability-level matching per D-11. Do not add tool-local callbacks, `Fn` hooks, or per-tool procedural filters here. The only policy input should be the static `AgentSurfaceSpec` stored on `ToolSpec` (D-12).

4. Add unit tests that build small synthetic clap trees and prove that:
   - the filtered tree removes an undeclared subcommand entirely
   - the filtered tree removes an undeclared flag entirely
   - the filtered tree leaves a declared command path and its declared flags intact
  </action>
  <acceptance_criteria>
    - `crates/cli-common/src/agent.rs` contains `fn apply_agent_surface`
    - `crates/cli-common/src/agent.rs` contains `fn visible_capabilities`
    - no tool-local callback type such as `Box<dyn Fn` is introduced for policy selection
    - tests cover undeclared subcommand removal and undeclared flag removal
    - `cargo test -p tftio-cli-common capability_policy --lib` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p tftio-cli-common capability_policy --lib</automated>
  </verify>
  <done>The shared policy model can materially prune a `clap` command tree from named capabilities instead of relying on tool-local logic.</done>
</task>

</tasks>

<verification>
- `cargo test -p tftio-cli-common agent_mode_activation --lib`
- `cargo test -p tftio-cli-common capability_policy --lib`
- `rg -n "TFTIO_AGENT_TOKEN|TFTIO_AGENT_TOKEN_EXPECTED|agent_surface" crates/cli-common/src`
</verification>

<success_criteria>
- Shared env-var names and fail-closed activation rules are defined once in `cli-common`
- `ToolSpec` can declare static capability groups for later rollout plans
- `cli-common` can prune a command tree from named capability declarations
</success_criteria>

<output>
After completion, create `.planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-01-SUMMARY.md`
</output>
