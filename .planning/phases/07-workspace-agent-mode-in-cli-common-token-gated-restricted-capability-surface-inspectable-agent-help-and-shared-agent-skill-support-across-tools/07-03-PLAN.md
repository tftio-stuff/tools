---
phase: 07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - crates/bsky-comment-extractor/src/cli.rs
  - crates/bsky-comment-extractor/src/main.rs
  - crates/bsky-comment-extractor/tests/agent_surface.rs
  - crates/gator/src/main.rs
  - crates/gator/src/cli.rs
  - crates/gator/tests/agent_surface.rs
autonomous: true
requirements: [D-04, D-05, D-06, D-07, D-08, D-09, D-13, D-14]

must_haves:
  truths:
    - "`bce` no longer relies on a one-off Phase 6 stub; it uses the shared agent surface from `cli-common`."
    - "`gator` exposes only its declared agent-launch surface when shared agent mode is active."
    - "Both tools reject hidden commands and flags as nonexistent in agent mode."
  artifacts:
    - path: "crates/bsky-comment-extractor/src/main.rs"
      provides: "Shared agent-aware entrypoint and removal of the local agent-help stub"
      contains: "parse_with_agent_surface"
    - path: "crates/gator/src/main.rs"
      provides: "Shared agent-aware entrypoint for gator"
      contains: "parse_with_agent_surface"
    - path: "crates/bsky-comment-extractor/tests/agent_surface.rs"
      provides: "Smoke coverage for `bce` restricted help and hidden-command rejection"
      contains: "query-posts"
  key_links:
    - from: "crates/bsky-comment-extractor/src/main.rs"
      to: "crates/cli-common/src/agent.rs"
      via: "shared agent-aware parse path"
      pattern: "parse_with_agent_surface"
    - from: "crates/gator/src/main.rs"
      to: "crates/gator/src/cli.rs"
      via: "capability declarations reference root launch args and visible flags"
      pattern: "run-agent"
---

<objective>
Migrate `bce` and `gator` onto the shared agent-mode substrate and replace the Phase 6 `bce --agent-help` one-off with the Phase 7 workspace implementation.

Purpose: This plan covers the absorbed Phase 6 scope (D-14) and proves the new substrate on one tool with an existing stub (`bce`) plus one tool with a root-command launch surface (`gator`).
Output: Shared agent-mode rollout for `bce` and `gator`, plus smoke tests for both tools.
</objective>

<execution_context>
@/Users/jfb/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jfb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-CONTEXT.md
@.planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-RESEARCH.md
@crates/bsky-comment-extractor/src/cli.rs
@crates/bsky-comment-extractor/src/main.rs
@crates/gator/src/cli.rs
@crates/gator/src/main.rs

<interfaces>
Shared helpers from Plan 07-02:
```rust
pub fn parse_with_agent_surface<T>(spec: &ToolSpec) -> Result<AgentDispatch<T>, clap::Error>;
pub fn render_agent_help(spec: &ToolSpec, ctx: &AgentModeContext) -> String;
```

Current `bce` local stub to remove:
```rust
#[arg(long, global = true, hide = true)]
pub agent_help: bool

fn print_agent_help() { ...pending_phase_06... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace `bce`'s local Phase 6 agent-help stub with the shared Phase 7 substrate</name>
  <read_first>
    - crates/bsky-comment-extractor/src/cli.rs
    - crates/bsky-comment-extractor/src/main.rs
    - .planning/PROJECT.md
    - .planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-CONTEXT.md
    - crates/cli-common/src/agent.rs
    - crates/cli-common/src/command.rs
  </read_first>
  <files>crates/bsky-comment-extractor/src/cli.rs, crates/bsky-comment-extractor/src/main.rs, crates/bsky-comment-extractor/tests/agent_surface.rs</files>
  <behavior>
    - Test 1: With `TFTIO_AGENT_TOKEN=phase7-test-token` and `TFTIO_AGENT_TOKEN_EXPECTED=phase7-test-token`, `bce --agent-help` prints a structured manifest containing `query-posts` and not `fetch` (D-04, D-07, D-09, D-14).
    - Test 2: Under the same env vars, `bce --agent-skill query-posts` prints only the `query-posts` capability contract (D-08).
    - Test 3: Under the same env vars, `bce fetch alice.bsky.social` fails as an unrecognized subcommand or hidden path instead of revealing the hidden surface (D-05, D-06).
  </behavior>
  <action>
1. In `crates/bsky-comment-extractor/src/cli.rs`, remove the local `agent_help` field entirely. `bce` must stop carrying its own one-off hidden flag because D-14 moves this feature into shared `cli-common`.

2. In `crates/bsky-comment-extractor/src/main.rs`, restore a conventional `fn main()` if the branch still lacks one, and route startup through the shared parser from Plan 07-02. Do not keep `print_agent_help()`.

3. Declare an agent surface on `TOOL_SPEC` with exactly one visible capability:
   - capability slug: `query-posts`
   - command path: `["query"]`
   - visible flags: `db`, `limit`, `offset`
   - examples: `bce query --limit 25`, `bce query --db /tmp/bsky.db --offset 50`
   - output summary: `stdout emits one JSON envelope line followed by JSON post lines`
   - constraint summary: `local SQLite only; no network; missing DB returns structured stderr JSON`
   This implements D-14 while intentionally keeping `fetch` out of agent mode.

4. Keep human mode unchanged: without the exact token match, `bce` still exposes its full fetch/query human CLI.

5. Add `crates/bsky-comment-extractor/tests/agent_surface.rs` covering `--agent-help`, `--agent-skill query-posts`, and hidden `fetch` rejection under the shared env vars.
  </action>
  <acceptance_criteria>
    - `crates/bsky-comment-extractor/src/cli.rs` no longer contains `pub agent_help: bool`
    - `crates/bsky-comment-extractor/src/main.rs` no longer contains `pending_phase_06`
    - `crates/bsky-comment-extractor/src/main.rs` contains `query-posts`
    - `crates/bsky-comment-extractor/src/main.rs` contains `parse_with_agent_surface`
    - `crates/bsky-comment-extractor/tests/agent_surface.rs` exists and asserts hidden `fetch` rejection under active agent mode
    - `cargo test -p tftio-bsky-comment-extractor agent_surface` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p tftio-bsky-comment-extractor agent_surface</automated>
  </verify>
  <done>`bce` uses the shared Phase 7 agent surface, exposes only `query-posts` in agent mode, and no longer carries a local Phase 6 stub.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate `gator` to the shared restricted agent-launch surface</name>
  <read_first>
    - crates/gator/src/cli.rs
    - crates/gator/src/main.rs
    - crates/cli-common/src/agent.rs
    - .planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-CONTEXT.md
  </read_first>
  <files>crates/gator/src/cli.rs, crates/gator/src/main.rs, crates/gator/tests/agent_surface.rs</files>
  <behavior>
    - Test 1: In agent mode, `gator --agent-help` exposes only the declared `run-agent` capability and not `meta` commands (D-04, D-07, D-09).
    - Test 2: In agent mode, `gator --agent-skill run-agent` prints only the `run-agent` capability contract (D-08).
    - Test 3: In agent mode, `gator meta version` fails as if `meta` does not exist (D-05, D-06).
    - Test 4: In agent mode, undeclared privileged flags such as `--add-dirs`, `--add-dirs-ro`, `--share-worktrees`, `--session`, and `--no-yolo` are absent from help and rejected at parse time (D-04, D-05, D-06).
  </behavior>
  <action>
1. In `crates/gator/src/main.rs`, replace direct `Cli::parse` startup with the shared parse helper from Plan 07-02 before entering the existing `run(&Cli)` flow.

2. In `crates/gator/src/cli.rs`, declare one visible capability on the `TOOL_SPEC` path from `main.rs`:
   - capability slug: `run-agent`
   - command path: the root launch path (no subcommand)
   - visible args/flags: `agent`, `profiles`, `workdir`, `policy`, `no-prompt`, `dry-run`, `json`, `agent_args`
   - hidden in agent mode: `meta`, `add-dirs`, `add-dirs-ro`, `session`, `share_worktrees`, `no_yolo`
   If no custom prose is needed beyond those selectors, rely on the shared default-generation strategy from Plan 07-02 for `summary`, `examples`, `output`, and `constraints`.
   This implements the restricted surface per D-04 through D-06.

3. Preserve existing human-mode validation and run behavior. The only difference should be the filtered surface when the exact shared token match is active.

4. Add `crates/gator/tests/agent_surface.rs` that activates the shared env vars and verifies:
   - `--agent-help` output contains `run-agent`
   - `--agent-skill run-agent` renders the single visible capability contract
   - `meta version` fails in agent mode
   - `--session` is rejected in agent mode
  </action>
  <acceptance_criteria>
    - `crates/gator/src/main.rs` contains `parse_with_agent_surface`
    - `crates/gator/src/main.rs` or `crates/gator/src/cli.rs` contains `run-agent`
    - the capability declaration lists `workdir`, `policy`, `no-prompt`, `dry-run`, `json`, and `agent_args`
    - the capability declaration does not expose `meta`, `session`, or `share_worktrees`
    - `crates/gator/tests/agent_surface.rs` asserts `--agent-skill run-agent`
    - `cargo test -p tftio-gator agent_surface` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p tftio-gator agent_surface</automated>
  </verify>
  <done>`gator` exposes only the declared restricted launch surface in agent mode while preserving its full human CLI when the token gate is inactive.</done>
</task>

<task type="auto">
  <name>Task 3: Run package-level smoke verification for the `bce` and `gator` rollout</name>
  <read_first>
    - crates/bsky-comment-extractor/tests/agent_surface.rs
    - crates/gator/tests/agent_surface.rs
    - .planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-VALIDATION.md
  </read_first>
  <files>crates/bsky-comment-extractor/tests/agent_surface.rs, crates/gator/tests/agent_surface.rs</files>
  <action>
1. Run the new package-level agent-surface tests for both crates.
2. Run one compile-level smoke command for each binary so this plan catches integration breakage early:
   - `cargo test -p tftio-bsky-comment-extractor agent_surface`
   - `cargo test -p tftio-gator agent_surface`
3. If either test requires a small fixture helper update inside the same test file, make that adjustment here and keep the test package-local.
  </action>
  <acceptance_criteria>
    - `cargo test -p tftio-bsky-comment-extractor agent_surface` exits 0
    - `cargo test -p tftio-gator agent_surface` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p tftio-bsky-comment-extractor agent_surface && cargo test -p tftio-gator agent_surface</automated>
  </verify>
  <done>Both migrated tools have passing package-local smoke coverage for their restricted agent surfaces.</done>
</task>

</tasks>

<verification>
- `cargo test -p tftio-bsky-comment-extractor agent_surface`
- `cargo test -p tftio-gator agent_surface`
- `rg -n "query-posts|run-agent|parse_with_agent_surface" crates/bsky-comment-extractor crates/gator`
</verification>

<success_criteria>
- `bce`'s absorbed Phase 6 agent-help scope is implemented via shared `cli-common`
- `gator` uses the shared restricted surface instead of exposing its full CLI in agent mode
- Both tools have package-local tests proving hidden-surface redaction
</success_criteria>

<output>
After completion, create `.planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-03-SUMMARY.md`
</output>
