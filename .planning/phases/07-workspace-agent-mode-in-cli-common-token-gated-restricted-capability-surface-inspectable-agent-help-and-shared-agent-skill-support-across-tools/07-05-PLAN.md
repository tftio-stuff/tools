---
phase: 07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools
plan: 05
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - crates/asana-cli/src/cli/mod.rs
  - crates/asana-cli/tests/agent_surface.rs
  - crates/silent-critic/src/main.rs
  - crates/silent-critic/tests/agent_surface.rs
autonomous: true
requirements: [D-04, D-05, D-06, D-07, D-08, D-09, D-13]

must_haves:
  truths:
    - "`asana-cli` exposes only its declared operational groups in agent mode."
    - "`silent-critic` exposes only worker-safe session commands in agent mode while keeping higher-role operations hidden."
    - "Shared env vars activate restricted agent mode across both tools without replacing Silent Critic's existing worker-session token checks."
  artifacts:
    - path: "crates/asana-cli/src/cli/mod.rs"
      provides: "Shared agent-aware entry path and Asana capability declarations"
      contains: "manage-tasks"
    - path: "crates/silent-critic/src/main.rs"
      provides: "Shared agent-aware entry path for worker-safe session commands"
      contains: "session-manifest"
    - path: "crates/silent-critic/tests/agent_surface.rs"
      provides: "Smoke coverage for hidden higher-role commands"
      contains: "session-manifest"
  key_links:
    - from: "crates/asana-cli/src/cli/mod.rs"
      to: "crates/cli-common/src/agent.rs"
      via: "shared parse/help pipeline"
      pattern: "parse_with_agent_surface"
    - from: "crates/silent-critic/src/main.rs"
      to: "crates/silent-critic/src/commands/session.rs"
      via: "shared agent mode gate is separate from the existing `SILENT_CRITIC_TOKEN` worker token used at runtime"
      pattern: "SILENT_CRITIC_TOKEN|session-manifest"
---

<objective>
Roll out shared Phase 7 agent mode to `asana-cli` and `silent-critic`.

Purpose: These tools cover the broadest command trees in the workspace and the highest-value visibility-boundary case. This plan validates that Phase 7 works for both multi-subcommand operational CLIs and the original Silent Critic trust-boundary reference.
Output: Shared agent-mode adoption for `asana-cli` and `silent-critic`, plus package-level smoke tests.
</objective>

<execution_context>
@/Users/jfb/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jfb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-CONTEXT.md
@.planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-RESEARCH.md
@docs/the_silent_critic_tooling_design.md
@docs/the-silent-critic-system-spec.md
@crates/asana-cli/src/cli/mod.rs
@crates/silent-critic/src/cli.rs
@crates/silent-critic/src/main.rs
@crates/silent-critic/src/commands/session.rs

<interfaces>
Silent Critic worker-safe session commands:
```rust
SessionCommand::Status
SessionCommand::Manifest
SessionCommand::Submit { criterion: String }
```

Asana top-level operational groups:
```rust
Config, Task, Project, Section, Tag, CustomField, Workspace, User
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate `asana-cli` to shared capability-scoped agent mode</name>
  <read_first>
    - crates/asana-cli/src/cli/mod.rs
    - crates/cli-common/src/agent.rs
    - .planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-RESEARCH.md
  </read_first>
  <files>crates/asana-cli/src/cli/mod.rs, crates/asana-cli/tests/agent_surface.rs</files>
  <behavior>
    - Test 1: In agent mode, `asana-cli --agent-help` lists only the declared capability groups `manage-config`, `manage-tasks`, `manage-projects`, `manage-sections`, `manage-tags`, `manage-custom-fields`, `manage-workspaces`, and `manage-users` (D-07, D-09).
    - Test 2: In agent mode, `asana-cli --agent-skill manage-tasks` prints only the task capability contract (D-08).
    - Test 3: In agent mode, `asana-cli doctor` and `asana-cli update` fail as hidden commands (D-04, D-05, D-06).
  </behavior>
  <action>
1. In `crates/asana-cli/src/cli/mod.rs`, replace the direct `Cli::parse()` startup at the top of `run()` with the shared parser helper from Plan 07-02.

2. Declare these exact capability slugs on the `asana-cli` `TOOL_SPEC`:
   - `manage-config`
   - `manage-tasks`
   - `manage-projects`
   - `manage-sections`
   - `manage-tags`
   - `manage-custom-fields`
   - `manage-workspaces`
   - `manage-users`
   Map each slug to its matching top-level subcommand path and document representative output/constraint text from the existing CLI behavior.

3. Do not declare `version`, `license`, `completions`, `manpage`, `doctor`, or `update` for agent mode.

4. Add `crates/asana-cli/tests/agent_surface.rs` covering `--agent-help`, `--agent-skill manage-tasks`, and hidden `doctor` rejection under the shared env vars.
  </action>
  <acceptance_criteria>
    - `crates/asana-cli/src/cli/mod.rs` contains `parse_with_agent_surface`
    - `crates/asana-cli/src/cli/mod.rs` contains all eight capability slugs listed above
    - no capability slug is declared for `doctor`, `update`, or `manpage`
    - `crates/asana-cli/tests/agent_surface.rs` exists
    - `cargo test -p tftio-asana-cli agent_surface` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p tftio-asana-cli agent_surface</automated>
  </verify>
  <done>`asana-cli` uses the shared Phase 7 substrate and exposes only its declared operational groups in agent mode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate `silent-critic` to the shared restricted worker surface</name>
  <read_first>
    - crates/silent-critic/src/cli.rs
    - crates/silent-critic/src/main.rs
    - crates/silent-critic/src/commands/session.rs
    - docs/the_silent_critic_tooling_design.md
    - docs/the-silent-critic-system-spec.md
    - docs/the_silent_critic_formal_appendix.md
    - crates/cli-common/src/agent.rs
  </read_first>
  <files>crates/silent-critic/src/main.rs, crates/silent-critic/tests/agent_surface.rs</files>
  <behavior>
    - Test 1: In agent mode, `silent-critic --agent-help` lists only `session-status`, `session-manifest`, and `session-submit` (D-04, D-07, D-09).
    - Test 2: In agent mode, `silent-critic --agent-skill session-manifest` prints only the `session-manifest` capability contract (D-08).
    - Test 3: In agent mode, higher-role commands such as `project init`, `criterion create`, `contract show`, `decide`, `log`, `session new`, `session discover`, `session sandbox`, `session compose-from`, and `session go` are absent and parse as nonexistent (D-04, D-05, D-06).
    - Test 4: Shared agent mode activation still remains separate from the existing `SILENT_CRITIC_TOKEN` worker token used inside `session::run_manifest` and `session::run_submit` (D-01, D-02, D-03).
  </behavior>
  <action>
1. In `crates/silent-critic/src/main.rs`, replace direct `Cli::parse()` startup with the shared parser helper from Plan 07-02 before dispatch.

2. Declare exactly these agent-visible capability slugs on the `silent-critic` `TOOL_SPEC`:
   - `session-status` → `session status`
   - `session-manifest` → `session manifest`
   - `session-submit` → `session submit --criterion <ID>`
   Rely on the shared default-generation strategy from Plan 07-02 for any capability prose fields not explicitly customized.

3. Keep all higher-role commands hidden in agent mode. Do not declare `project`, `criterion`, `contract`, `decide`, `log`, `session new`, `session discover`, `session sandbox`, `session compose-from`, or `session go`.

4. Do not replace `SILENT_CRITIC_TOKEN` inside `crates/silent-critic/src/commands/session.rs`. The shared env vars (`TFTIO_AGENT_TOKEN`, `TFTIO_AGENT_TOKEN_EXPECTED`) only gate the visible CLI surface per D-01 and D-02; the existing worker-session token continues to authorize `run_manifest` and `run_submit`.

5. Add `crates/silent-critic/tests/agent_surface.rs` covering the restricted help output, `--agent-skill session-manifest`, and hidden higher-role command rejection.
  </action>
  <acceptance_criteria>
    - `crates/silent-critic/src/main.rs` contains `parse_with_agent_surface`
    - `crates/silent-critic/src/main.rs` contains `session-status`, `session-manifest`, and `session-submit`
    - `crates/silent-critic/src/main.rs` does not declare `project init` or `criterion create` as agent capabilities
    - `crates/silent-critic/src/commands/session.rs` still contains `SILENT_CRITIC_TOKEN`
    - `crates/silent-critic/tests/agent_surface.rs` exists
    - `crates/silent-critic/tests/agent_surface.rs` asserts `--agent-skill session-manifest`
    - `cargo test -p tftio-silent-critic agent_surface` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p tftio-silent-critic agent_surface</automated>
  </verify>
  <done>`silent-critic` uses the shared Phase 7 gate to expose only worker-safe session commands while preserving its existing runtime worker-token checks.</done>
</task>

<task type="auto">
  <name>Task 3: Run package-level smoke verification for the `asana-cli` and `silent-critic` rollout</name>
  <read_first>
    - crates/asana-cli/tests/agent_surface.rs
    - crates/silent-critic/tests/agent_surface.rs
    - .planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-VALIDATION.md
  </read_first>
  <files>crates/asana-cli/tests/agent_surface.rs, crates/silent-critic/tests/agent_surface.rs</files>
  <action>
1. Run the new package-local smoke tests:
   - `cargo test -p tftio-asana-cli agent_surface`
   - `cargo test -p tftio-silent-critic agent_surface`
2. Keep any resulting fixture tweaks local to these test files.
  </action>
  <acceptance_criteria>
    - `cargo test -p tftio-asana-cli agent_surface` exits 0
    - `cargo test -p tftio-silent-critic agent_surface` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p tftio-asana-cli agent_surface && cargo test -p tftio-silent-critic agent_surface</automated>
  </verify>
  <done>`asana-cli` and `silent-critic` both have passing package-local smoke coverage for their restricted agent surfaces.</done>
</task>

</tasks>

<verification>
- `cargo test -p tftio-asana-cli agent_surface`
- `cargo test -p tftio-silent-critic agent_surface`
- `rg -n "manage-tasks|session-manifest|parse_with_agent_surface" crates/asana-cli crates/silent-critic`
</verification>

<success_criteria>
- `asana-cli` and `silent-critic` both use the shared agent-mode parser/help pipeline
- `silent-critic` keeps higher-role commands hidden in agent mode
- both tools have package-local smoke tests proving hidden-surface redaction
</success_criteria>

<output>
After completion, create `.planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-05-SUMMARY.md`
</output>
