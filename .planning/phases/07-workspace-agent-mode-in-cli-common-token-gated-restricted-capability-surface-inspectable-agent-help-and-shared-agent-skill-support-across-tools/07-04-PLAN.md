---
phase: 07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools
plan: 04
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - crates/todoer/src/main.rs
  - crates/todoer/tests/agent_surface.rs
  - crates/unvenv/src/main.rs
  - crates/unvenv/tests/agent_surface.rs
autonomous: true
requirements: [D-04, D-05, D-06, D-07, D-08, D-09, D-13]

must_haves:
  truths:
    - "`todoer` exposes only its declared task-management capabilities in agent mode."
    - "`unvenv` exposes only its scan workflow in agent mode."
    - "Both tools reject hidden metadata and privileged commands as nonexistent under the shared token gate."
  artifacts:
    - path: "crates/todoer/src/main.rs"
      provides: "Shared agent-aware entrypoint and capability declarations for Todoer"
      contains: "list-tasks"
    - path: "crates/unvenv/src/main.rs"
      provides: "Shared agent-aware entrypoint and restricted scan capability"
      contains: "scan-venvs"
    - path: "crates/unvenv/tests/agent_surface.rs"
      provides: "Smoke coverage for hidden doctor/update rejection"
      contains: "scan-venvs"
  key_links:
    - from: "crates/todoer/src/main.rs"
      to: "crates/cli-common/src/agent.rs"
      via: "shared parse and help pipeline"
      pattern: "parse_with_agent_surface"
    - from: "crates/unvenv/src/main.rs"
      to: "crates/cli-common/src/agent.rs"
      via: "shared parse and help pipeline"
      pattern: "parse_with_agent_surface"
---

<objective>
Roll out the shared Phase 7 agent surface to `todoer` and `unvenv`.

Purpose: These two tools validate the shared model across one JSON-centric task manager and one single-workflow scanner. Together they prove that capability declarations scale across different CLI shapes without tool-local policy logic.
Output: Shared agent-mode adoption for `todoer` and `unvenv`, plus package-level smoke tests.
</objective>

<execution_context>
@/Users/jfb/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jfb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-CONTEXT.md
@.planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-RESEARCH.md
@crates/todoer/src/cli.rs
@crates/todoer/src/main.rs
@crates/unvenv/src/main.rs
@crates/cli-common/src/agent.rs

<interfaces>
Todoer command paths:
```rust
Init
New
List
Task::Status
Task::Show
Task::Note
Task::Update::Status
```

Unvenv command paths:
```rust
Scan
Version
License
Completions
Doctor
Update
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate `todoer` to shared agent-mode capability declarations</name>
  <read_first>
    - crates/todoer/src/cli.rs
    - crates/todoer/src/main.rs
    - crates/cli-common/src/agent.rs
    - .planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-CONTEXT.md
  </read_first>
  <files>crates/todoer/src/main.rs, crates/todoer/tests/agent_surface.rs</files>
  <behavior>
    - Test 1: In agent mode, `todoer --agent-help` lists the declared capabilities `init-project`, `create-task`, `list-tasks`, `task-status`, `task-show`, `task-note`, and `update-task-status` (D-07, D-09).
    - Test 2: In agent mode, `todoer --agent-skill list-tasks` prints only the `list-tasks` contract (D-08).
    - Test 3: In agent mode, `todoer meta version` fails as a hidden path and does not reveal the hidden metadata surface (D-04, D-05, D-06).
  </behavior>
  <action>
1. In `crates/todoer/src/main.rs`, switch startup to the shared parser helper from Plan 07-02 before entering the existing `run(cli)` dispatcher.

2. Declare these exact capabilities on the `todoer` `TOOL_SPEC`:
   - `init-project` → `init --project`
   - `create-task` → `new [description] --project --json`
   - `list-tasks` → `list --project --all --json`
   - `task-status` → `task status <id> --json`
   - `task-show` → `task show <id> --json`
   - `task-note` → `task note <id> <note> --json`
   - `update-task-status` → `task update status <id> <status> --json`
   Rely on the shared default-generation strategy from Plan 07-02 for any capability prose fields not explicitly customized.

3. Do not expose `meta` in agent mode. Human mode remains unchanged.

4. Add `crates/todoer/tests/agent_surface.rs` covering `--agent-help`, `--agent-skill list-tasks`, and hidden `meta version` rejection under `TFTIO_AGENT_TOKEN=phase7-test-token` and `TFTIO_AGENT_TOKEN_EXPECTED=phase7-test-token`.
  </action>
  <acceptance_criteria>
    - `crates/todoer/src/main.rs` contains `parse_with_agent_surface`
    - `crates/todoer/src/main.rs` contains all declared capability slugs listed above
    - `crates/todoer/tests/agent_surface.rs` exists
    - tests assert hidden `meta version` rejection under active agent mode
    - `cargo test -p tftio-todoer agent_surface` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p tftio-todoer agent_surface</automated>
  </verify>
  <done>`todoer` exposes a capability-scoped task-management surface in agent mode and hides its metadata commands there.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate `unvenv` to a restricted scan-only agent surface</name>
  <read_first>
    - crates/unvenv/src/main.rs
    - crates/cli-common/src/agent.rs
    - .planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-RESEARCH.md
  </read_first>
  <files>crates/unvenv/src/main.rs, crates/unvenv/tests/agent_surface.rs</files>
  <behavior>
    - Test 1: In agent mode, `unvenv --agent-help` lists only `scan-venvs` and omits `doctor`, `update`, and `completions` (D-04, D-07, D-09).
    - Test 2: In agent mode, `unvenv --agent-skill scan-venvs` prints the scan contract (D-08).
    - Test 3: In agent mode, `unvenv doctor` fails as a hidden command (D-05, D-06).
  </behavior>
  <action>
1. In `crates/unvenv/src/main.rs`, replace direct `Cli::parse()` startup with the shared parser helper from Plan 07-02.

2. Declare one visible capability on the `unvenv` `TOOL_SPEC`:
   - capability slug: `scan-venvs`
   - command path: `["scan"]`
   - visible flags/args: none beyond the subcommand itself
   - output summary: plain-text or non-zero exit based on unignored `pyvenv.cfg` detection
   - constraints: reads the current working tree only
   Use the shared default-generation strategy from Plan 07-02 for `summary` or `examples` if you do not add custom prose.

3. Keep `version`, `license`, `completions`, `doctor`, and `update` available only in human mode. Do not declare them in the agent surface.

4. Add `crates/unvenv/tests/agent_surface.rs` covering `--agent-help`, `--agent-skill scan-venvs`, and hidden `doctor` rejection under the shared env vars.
  </action>
  <acceptance_criteria>
    - `crates/unvenv/src/main.rs` contains `parse_with_agent_surface`
    - `crates/unvenv/src/main.rs` contains `scan-venvs`
    - `crates/unvenv/tests/agent_surface.rs` exists
    - tests assert hidden `doctor` rejection under active agent mode
    - `cargo test -p tftio-unvenv agent_surface` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p tftio-unvenv agent_surface</automated>
  </verify>
  <done>`unvenv` exposes only its scan workflow in agent mode and hides metadata/maintenance commands there.</done>
</task>

<task type="auto">
  <name>Task 3: Run package-level smoke verification for the `todoer` and `unvenv` rollout</name>
  <read_first>
    - crates/todoer/tests/agent_surface.rs
    - crates/unvenv/tests/agent_surface.rs
    - .planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-VALIDATION.md
  </read_first>
  <files>crates/todoer/tests/agent_surface.rs, crates/unvenv/tests/agent_surface.rs</files>
  <action>
1. Run the new package-local smoke tests:
   - `cargo test -p tftio-todoer agent_surface`
   - `cargo test -p tftio-unvenv agent_surface`
2. If either test needs a small fixture helper tweak, keep the fix local to the corresponding test file.
  </action>
  <acceptance_criteria>
    - `cargo test -p tftio-todoer agent_surface` exits 0
    - `cargo test -p tftio-unvenv agent_surface` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p tftio-todoer agent_surface && cargo test -p tftio-unvenv agent_surface</automated>
  </verify>
  <done>`todoer` and `unvenv` both have passing package-local smoke coverage for their restricted agent surfaces.</done>
</task>

</tasks>

<verification>
- `cargo test -p tftio-todoer agent_surface`
- `cargo test -p tftio-unvenv agent_surface`
- `rg -n "list-tasks|scan-venvs|parse_with_agent_surface" crates/todoer crates/unvenv`
</verification>

<success_criteria>
- `todoer` and `unvenv` both use the shared agent-mode parser/help pipeline
- only declared capabilities remain visible in agent mode
- both tools have package-local smoke tests proving hidden-surface redaction
</success_criteria>

<output>
After completion, create `.planning/phases/07-workspace-agent-mode-in-cli-common-token-gated-restricted-capability-surface-inspectable-agent-help-and-shared-agent-skill-support-across-tools/07-04-SUMMARY.md`
</output>
