---
phase: 07-add-shared-agent-help-and-agent-skill-support-across-cli-crates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cli-common/src/lib.rs
  - crates/cli-common/src/agent_docs.rs
autonomous: true
requirements:
  - ADOC-01
  - ADOC-03
  - ADOC-04
must_haves:
  truths:
    - "A single cli-common API can render canonical YAML help and Claude-style skill output from the same agent-doc source data."
    - "Top-level-only detection rejects subcommand placements such as `tool subcommand --agent-help`."
    - "Shared validation helpers keep each crate's authored agent docs aligned with the real clap command and argument surface."
  artifacts:
    - path: "crates/cli-common/src/agent_docs.rs"
      provides: "Shared request detection, agent-doc model, YAML renderer, skill renderer, and coverage helpers."
    - path: "crates/cli-common/src/lib.rs"
      provides: "Public exports for the shared agent-doc API."
  key_links:
    - from: "crates/cli-common/src/lib.rs"
      to: "crates/cli-common/src/agent_docs.rs"
      via: "pub mod and re-exports"
      pattern: "pub mod agent_docs|pub use agent_docs"
    - from: "crates/cli-common/src/agent_docs.rs"
      to: "workspace binary crates"
      via: "detect_agent_doc_request and renderer functions"
      pattern: "detect_agent_doc_request|render_agent_help_yaml|render_agent_skill"
---

<objective>
Create the shared `cli-common` contract for agent-facing documentation so every binary can use one source of truth.

Purpose: Freeze the canonical schema and shared behavior before touching individual crates.
Output: A reusable `agent_docs` module with detection, rendering, and validation helpers.
</objective>

<execution_context>
@/Users/jfb/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jfb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-CONTEXT.md
@.planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-RESEARCH.md
@.planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-VALIDATION.md
@CLAUDE.md
@crates/cli-common/src/lib.rs
@crates/cli-common/src/output.rs

<interfaces>
From `crates/cli-common/src/lib.rs`:
```rust
pub mod completions;
pub mod doctor;
pub mod license;
pub mod output;
pub mod types;
pub mod update;
```

Target shared API to create and export from `cli-common`:
```rust
pub enum AgentDocRequest { Help, Skill }
pub fn detect_agent_doc_request<I, S>(args: I) -> Option<AgentDocRequest>
where
    I: IntoIterator<Item = S>,
    S: AsRef<std::ffi::OsStr>;
pub fn render_agent_help_yaml(doc: &AgentDoc) -> String;
pub fn render_agent_skill(doc: &AgentDoc) -> String;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the shared agent-doc model, renderers, and top-level detector</name>
  <files>crates/cli-common/src/agent_docs.rs, crates/cli-common/src/lib.rs</files>
  <read_first>.planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-CONTEXT.md, .planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-RESEARCH.md, CLAUDE.md, crates/cli-common/src/lib.rs, crates/cli-common/src/output.rs, crates/bsky-comment-extractor/src/cli.rs, crates/bsky-comment-extractor/src/main.rs</read_first>
  <acceptance_criteria>
    - `rg -n "pub (mod|use) agent_docs" crates/cli-common/src/lib.rs` shows the new module is exported.
    - `rg -n "enum AgentDocRequest|struct AgentDoc|fn detect_agent_doc_request|fn render_agent_help_yaml|fn render_agent_skill" crates/cli-common/src/agent_docs.rs` finds all shared entry points.
    - `cargo test -p tftio-cli-common agent_docs --lib` passes and includes assertions for subcommand rejection, canonical YAML ordering, and shared-source skill rendering.
  </acceptance_criteria>
  <behavior>
    - Test 1: top-level `--agent-help` and `--agent-skill` map to `AgentDocRequest`, while `tool subcommand --agent-help` returns `None` per D-04, D-06, D-07, and D-08.
    - Test 2: one `AgentDoc` instance renders canonical YAML with exhaustive top-level sections and stable field order per D-01 and D-02.
    - Test 3: `render_agent_skill` uses the same `AgentDoc` input and emits YAML front matter plus markdown body without content drift per D-04 and D-05.
  </behavior>
  <action>Create `crates/cli-common/src/agent_docs.rs` with a documented data model that can express shared inherited sections plus per-tool sections: tool metadata, commands, arguments, environment variables, config files, default paths, output shapes, examples, failure modes, and operator-mistake guidance per D-01, D-02, and D-03. Implement `detect_agent_doc_request` as a raw-argv top-level detector that succeeds only for `tool --agent-help` and `tool --agent-skill`, rejects any invocation where another positional or subcommand token appears before the flag, and treats both flags as mutually exclusive top-level requests per D-06, D-07, and D-08. Implement `render_agent_help_yaml` with explicit string assembly or a maintained YAML-safe serializer strategy that preserves the canonical schema order and does not add a second authored source. Implement `render_agent_skill` by reusing the same `AgentDoc` data: YAML front matter first, then markdown sections for usage, examples, constraints, and errors per D-04 and D-05. Export the new module and its public API from `crates/cli-common/src/lib.rs`.</action>
  <verify>
    <automated>cargo test -p tftio-cli-common agent_docs --lib</automated>
  </verify>
  <done>The shared module exists, compiles, and provides one documented source-of-truth API that downstream crates can call for both `--agent-help` and `--agent-skill`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add shared clap-coverage helpers and lock the phase schema with tests</name>
  <files>crates/cli-common/src/agent_docs.rs</files>
  <read_first>.planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-CONTEXT.md, .planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-RESEARCH.md, crates/cli-common/src/agent_docs.rs, crates/asana-cli/src/cli/mod.rs, crates/todoer/src/cli.rs, crates/gator/src/cli.rs</read_first>
  <acceptance_criteria>
    - `rg -n "fn assert_command_coverage|fn assert_argument_coverage|CommandFactory" crates/cli-common/src/agent_docs.rs` shows reusable drift-check helpers.
    - `cargo test -p tftio-cli-common agent_docs --lib` passes with tests that compare authored docs against clap command/argument names.
    - No new dependency is added outside workspace conventions unless `Cargo.toml` diff shows a workspace-level entry justified by the shared renderer implementation.
  </acceptance_criteria>
  <behavior>
    - Test 1: helper assertions fail if a documented command or flag is missing from authored metadata.
    - Test 2: helpers allow hidden agent-doc flags to remain undocumented in normal help while still validating the real clap tree.
  </behavior>
  <action>Extend `crates/cli-common/src/agent_docs.rs` with validation helpers built around `clap::CommandFactory`, `get_subcommands()`, and `get_arguments()` so each binary can assert that authored agent docs cover the actual parser surface without trying to auto-generate semantics from clap metadata. Include helper APIs for top-level commands, nested subcommands, long flags, and positional argument names. Add focused unit tests using small fake clap parsers to prove the helpers catch missing command coverage and accept intentionally hidden agent-doc flags. Keep the helper surface in `cli-common` so Phase 7 crates all consume the same drift-check behavior per D-01, D-03, and D-10.</action>
  <verify>
    <automated>cargo test -p tftio-cli-common agent_docs --lib</automated>
  </verify>
  <done>`cli-common` exposes validation helpers that later crate plans can reuse to keep authored agent docs aligned with clap command trees.</done>
</task>

</tasks>

<verification>
Run `cargo test -p tftio-cli-common agent_docs --lib` after each task and confirm the shared API rejects subcommand placement while rendering both output formats from the same model.
</verification>

<success_criteria>
`cli-common` defines the canonical Phase 7 contract: one model, two renderers, top-level-only request detection, and reusable coverage helpers for downstream crates.
</success_criteria>

<output>
After completion, create `.planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-01-SUMMARY.md`
</output>
