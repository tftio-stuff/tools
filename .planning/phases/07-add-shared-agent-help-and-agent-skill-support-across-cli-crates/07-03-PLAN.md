---
phase: 07-add-shared-agent-help-and-agent-skill-support-across-cli-crates
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - crates/todoer/src/cli.rs
  - crates/todoer/src/main.rs
  - crates/todoer/tests/agent_help.rs
  - crates/silent-critic/src/cli.rs
  - crates/silent-critic/src/main.rs
  - crates/silent-critic/tests/agent_help.rs
autonomous: true
requirements:
  - ADOC-02
  - ADOC-03
  - ADOC-04
  - ADOC-05
must_haves:
  truths:
    - "`todoer` and `silent-critic` expose exhaustive agent docs for nested subcommands, JSON output modes, config files, and SQLite-backed behavior."
    - "Both crates preserve their existing subcommand-required flows when agent-doc flags are absent."
    - "Hidden flags stay out of normal help output while top-level requests exit `0` and write to stdout."
  artifacts:
    - path: "crates/todoer/tests/agent_help.rs"
      provides: "Subprocess coverage for project-scoped todo commands and JSON mode documentation."
    - path: "crates/silent-critic/tests/agent_help.rs"
      provides: "Subprocess coverage for deeply nested session and criterion command docs."
  key_links:
    - from: "crates/todoer/src/main.rs"
      to: "crates/todoer/src/cli.rs"
      via: "top-level doc short-circuit before clap-required subcommand dispatch"
      pattern: "detect_agent_doc_request|Cli::parse"
    - from: "crates/silent-critic/src/main.rs"
      to: "crates/silent-critic/src/cli.rs"
      via: "pre-dispatch agent-doc path"
      pattern: "detect_agent_doc_request|dispatch"
---

<objective>
Apply the shared agent-doc system to the two stateful, nested-subcommand crates in the workspace.

Purpose: Cover crates whose docs must describe SQLite/config-driven behavior, JSON envelopes, and multi-level subcommand trees.
Output: Exhaustive per-tool docs and regression tests for `todoer` and `silent-critic`.
</objective>

<execution_context>
@/Users/jfb/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jfb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-CONTEXT.md
@.planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-RESEARCH.md
@.planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-VALIDATION.md
@CLAUDE.md
@crates/todoer/src/cli.rs
@crates/todoer/src/main.rs
@crates/silent-critic/src/cli.rs
@crates/silent-critic/src/main.rs
@crates/cli-common/src/agent_docs.rs

<interfaces>
`todoer` top-level parser:
```rust
pub struct Cli {
    pub command: Command,
}
```

`silent-critic` top-level parser:
```rust
pub struct Cli {
    pub json: bool,
    pub command: Command,
}
```

Shared API from Plan 07-01:
```rust
pub fn detect_agent_doc_request<I, S>(args: I) -> Option<AgentDocRequest>;
pub fn render_agent_help_yaml(doc: &AgentDoc) -> String;
pub fn render_agent_skill(doc: &AgentDoc) -> String;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add exhaustive agent-doc output to todoer and cover JSON/project behavior</name>
  <files>crates/todoer/src/cli.rs, crates/todoer/src/main.rs, crates/todoer/tests/agent_help.rs</files>
  <read_first>.planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-CONTEXT.md, .planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-RESEARCH.md, crates/cli-common/src/agent_docs.rs, crates/todoer/src/cli.rs, crates/todoer/src/main.rs, crates/todoer/src/output.rs, crates/todoer/src/config.rs</read_first>
  <acceptance_criteria>
    - `rg -n "agent_skill|--agent-skill|detect_agent_doc_request" crates/todoer/src/main.rs crates/todoer/src/cli.rs` shows the top-level doc path.
    - `cargo test -p tftio-todoer agent_help` passes and asserts success for top-level `--agent-help` / `--agent-skill` plus hidden-help behavior.
    - The `todoer` agent doc output includes `.todoer.toml`, project resolution rules, JSON output mode, and `task update status` command coverage; verify with `cargo test -p tftio-todoer agent_help -- --nocapture` or string assertions inside the test file.
  </acceptance_criteria>
  <behavior>
    - Test 1: top-level agent-doc requests succeed without requiring a subcommand.
    - Test 2: authored docs enumerate `init`, `new`, `list`, `task status`, `task show`, `task note`, and `task update status` with args, JSON output shapes, config file usage, and operator-mistake guidance.
    - Test 3: `todoer list --help` and other normal help paths omit the hidden agent-doc flags.
  </behavior>
  <action>Add top-level raw-argv interception to `todoer` before `Cli::parse()`. Author a `todoer`-specific `AgentDoc` that covers project discovery, `.todoer.toml`, JSON output envelopes from `ok_response` and `err_response`, command-specific arguments, likely operator mistakes around project resolution and stdin note input, and SQLite-backed behavior per D-02, D-03, D-05, and D-09. Keep standard subcommand parsing unchanged for all non-agent-doc invocations. Add subprocess tests in `crates/todoer/tests/agent_help.rs` that assert top-level success, hidden help, and subcommand placement rejection.</action>
  <verify>
    <automated>cargo test -p tftio-todoer agent_help</automated>
  </verify>
  <done>`todoer` emits exhaustive agent docs in both required formats and retains its existing command behavior outside the top-level doc path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add agent-doc support to silent-critic for the full session/criterion command tree</name>
  <files>crates/silent-critic/src/cli.rs, crates/silent-critic/src/main.rs, crates/silent-critic/tests/agent_help.rs</files>
  <read_first>.planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-CONTEXT.md, .planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-RESEARCH.md, crates/cli-common/src/agent_docs.rs, crates/silent-critic/src/cli.rs, crates/silent-critic/src/main.rs, crates/silent-critic/src/output.rs, crates/silent-critic/src/config.rs</read_first>
  <acceptance_criteria>
    - `rg -n "agent_skill|--agent-skill|detect_agent_doc_request" crates/silent-critic/src/main.rs crates/silent-critic/src/cli.rs` shows the shared integration.
    - `cargo test -p tftio-silent-critic agent_help` passes and verifies nested command coverage and hidden-help behavior.
    - The tests assert that agent-doc output includes session-state-machine guidance, `SILENT_CRITIC_TOKEN`, config/db paths, JSON export/log formats, and likely operator mistakes called out in the doc content.
  </acceptance_criteria>
  <behavior>
    - Test 1: top-level `silent-critic --agent-help` and `--agent-skill` succeed without a subcommand and exit `0`.
    - Test 2: the shared-source docs enumerate project, criterion, session, contract, decide, and log flows with required args and output contracts.
    - Test 3: `silent-critic session --agent-help` does not return agent docs and normal help output omits the hidden flags.
  </behavior>
  <action>Integrate the shared detector ahead of `Cli::parse()` or dispatch in `crates/silent-critic/src/main.rs`, and author a tool-specific `AgentDoc` that covers all nested command groups, JSON mode, config/database paths, session token handling, session lifecycle states, export/log formats, and common operator mistakes per D-02, D-03, D-05, and D-09. Use the `cli-common` validation helpers to keep the authored doc aligned with the clap tree rather than auto-generating prose. Add `crates/silent-critic/tests/agent_help.rs` with subprocess assertions for top-level success, hidden flags, and rejection of subcommand placement.</action>
  <verify>
    <automated>cargo test -p tftio-silent-critic agent_help</automated>
  </verify>
  <done>`silent-critic` exposes exhaustive agent docs for its nested command tree without changing ordinary JSON or plain-text execution behavior.</done>
</task>

</tasks>

<verification>
Run `cargo test -p tftio-todoer agent_help && cargo test -p tftio-silent-critic agent_help` and confirm both crates document config/db paths, JSON behavior, and hidden top-level-only agent-doc flags.
</verification>

<success_criteria>
The stateful workspace crates now satisfy the Phase 7 contract with exhaustive authored docs, top-level-only requests, and regression coverage around nested command trees.
</success_criteria>

<output>
After completion, create `.planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-03-SUMMARY.md`
</output>
