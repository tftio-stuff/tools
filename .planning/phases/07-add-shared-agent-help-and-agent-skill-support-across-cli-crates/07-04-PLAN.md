---
phase: 07-add-shared-agent-help-and-agent-skill-support-across-cli-crates
plan: 04
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
  - 07-03
files_modified:
  - crates/prompter/src/lib.rs
  - crates/prompter/src/main.rs
  - crates/prompter/tests/agent_help.rs
  - crates/asana-cli/src/cli/mod.rs
  - crates/asana-cli/src/main.rs
  - crates/asana-cli/tests/agent_help.rs
autonomous: true
requirements:
  - ADOC-02
  - ADOC-03
  - ADOC-04
  - ADOC-05
must_haves:
  truths:
    - "`prompter` and `asana-cli` expose exhaustive agent docs despite their custom parser and deep async subcommand trees."
    - "Agent-doc requests bypass required parsing without changing ordinary CLI UX, help text, or error handling."
    - "All seven Phase 7 binaries are covered once this plan completes."
  artifacts:
    - path: "crates/prompter/tests/agent_help.rs"
      provides: "Regression coverage for custom `parse_args_from` / `AppMode` interception."
    - path: "crates/asana-cli/tests/agent_help.rs"
      provides: "Regression coverage for the largest clap tree and hidden flag behavior."
  key_links:
    - from: "crates/prompter/src/main.rs"
      to: "crates/prompter/src/lib.rs"
      via: "raw argv inspection before AppMode parsing"
      pattern: "env::args|detect_agent_doc_request|parse_args_from"
    - from: "crates/asana-cli/src/main.rs"
      to: "crates/asana-cli/src/cli/mod.rs"
      via: "agent-doc short-circuit before Cli::parse and run()"
      pattern: "detect_agent_doc_request|run_agent_doc_request|Cli::parse"
---

<objective>
Finish the Phase 7 rollout by wiring the two crates with the most specialized parser shapes and widest documentation surface.

Purpose: Close the rollout on the custom-parser (`prompter`) and deepest clap tree (`asana-cli`) cases so all seven binaries support the shared agent-doc contract.
Output: Exhaustive docs, top-level-only behavior, and regression tests for `prompter` and `asana-cli`.
</objective>

<execution_context>
@/Users/jfb/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jfb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-CONTEXT.md
@.planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-RESEARCH.md
@.planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-VALIDATION.md
@CLAUDE.md
@crates/prompter/src/lib.rs
@crates/prompter/src/main.rs
@crates/asana-cli/src/cli/mod.rs
@crates/asana-cli/src/main.rs
@crates/cli-common/src/agent_docs.rs

<interfaces>
`prompter` custom parse entry:
```rust
fn parse_args() -> Result<AppMode, String> {
    let args: Vec<String> = env::args().collect();
    parse_args_from(args)
}
```

`asana-cli` entrypoint:
```rust
pub fn run() -> Result<i32> {
    let cli = Cli::parse();
    // dispatch follows
}
```

Shared API from Plan 07-01:
```rust
pub fn detect_agent_doc_request<I, S>(args: I) -> Option<AgentDocRequest>;
pub fn render_agent_help_yaml(doc: &AgentDoc) -> String;
pub fn render_agent_skill(doc: &AgentDoc) -> String;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add top-level agent-doc interception to prompter's custom parser flow</name>
  <files>crates/prompter/src/lib.rs, crates/prompter/src/main.rs, crates/prompter/tests/agent_help.rs</files>
  <read_first>.planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-CONTEXT.md, .planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-RESEARCH.md, crates/cli-common/src/agent_docs.rs, crates/prompter/src/lib.rs, crates/prompter/src/main.rs, crates/prompter/tests/cli.rs</read_first>
  <acceptance_criteria>
    - `rg -n "agent_skill|--agent-skill|detect_agent_doc_request" crates/prompter/src/lib.rs crates/prompter/src/main.rs` shows the interception path and doc builder.
    - `cargo test -p tftio-prompter agent_help` passes, including success for top-level requests and hidden-help assertions.
    - Tests assert that docs cover `run`, `list`, `tree`, `validate`, `init`, `completions`, `doctor`, config overrides, JSON mode, and recursive profile behavior.
  </acceptance_criteria>
  <behavior>
    - Test 1: `prompter --agent-help` and `--agent-skill` succeed before `parse_args_from` enforces a subcommand.
    - Test 2: authored docs cover config/library paths, recursive dependency rendering, JSON mode, and common operator mistakes like duplicate profile names or missing library files.
    - Test 3: `prompter run profile --agent-help` does not emit agent docs and normal help omits hidden flags.
  </behavior>
  <action>Hook `detect_agent_doc_request` into `prompter` before `parse_args_from` so agent-doc requests bypass the custom `AppMode` parse flow without changing normal behavior. Author a `prompter`-specific `AgentDoc` that covers every subcommand, `--config`, `--json`, render-time separator and prompt options, library/config file expectations, output modes, and likely operator mistakes per D-02, D-03, D-05, and D-09. Add `crates/prompter/tests/agent_help.rs` using the existing subprocess test style from `tests/cli.rs`.</action>
  <verify>
    <automated>cargo test -p tftio-prompter agent_help</automated>
  </verify>
  <done>`prompter` emits exhaustive agent docs from the top level without disturbing its custom parse-to-AppMode flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire asana-cli's deep clap tree into the shared agent-doc contract</name>
  <files>crates/asana-cli/src/cli/mod.rs, crates/asana-cli/src/main.rs, crates/asana-cli/tests/agent_help.rs</files>
  <read_first>.planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-CONTEXT.md, .planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-RESEARCH.md, crates/cli-common/src/agent_docs.rs, crates/asana-cli/src/cli/mod.rs, crates/asana-cli/src/main.rs, crates/asana-cli/tests/cli.rs, crates/asana-cli/src/config.rs</read_first>
  <acceptance_criteria>
    - `rg -n "agent_skill|--agent-skill|detect_agent_doc_request" crates/asana-cli/src/main.rs crates/asana-cli/src/cli/mod.rs` shows the top-level shared integration.
    - `cargo test -p tftio-asana-cli agent_help` passes, including success for top-level doc requests and hidden-help assertions.
    - Tests assert that docs cover config token management, task/project/section/tag/custom-field/workspace/user command groups, async API behavior, output formats, env vars, and likely operator mistakes.
  </acceptance_criteria>
  <behavior>
    - Test 1: top-level `asana-cli --agent-help` and `--agent-skill` succeed without a subcommand and exit `0`.
    - Test 2: the shared-source docs enumerate all clap command groups, common flags, config commands, manpage/completions/doctor/update flows, and major output shapes.
    - Test 3: `asana-cli task list --agent-help` does not trigger agent docs and human-facing help omits hidden flags.
  </behavior>
  <action>Introduce a top-level agent-doc short-circuit in `asana-cli` before `Cli::parse()` or at the earliest safe entrypoint so the large clap tree keeps its existing required-subcommand behavior. Author an `asana-cli` `AgentDoc` that explicitly covers every command group, config/env inputs (`ASANA_PAT` and related config fields), cache/config file locations, async/network error modes, output formats, and operator mistakes instead of trying to auto-generate prose from clap metadata per D-02, D-03, D-05, and D-09. Reuse the `cli-common` validation helpers to prove the authored docs cover the clap tree. Add `crates/asana-cli/tests/agent_help.rs` alongside the existing CLI subprocess suite.</action>
  <verify>
    <automated>cargo test -p tftio-asana-cli agent_help</automated>
  </verify>
  <done>`asana-cli` emits exhaustive top-level agent docs in both formats while preserving its existing command tree and human-facing help.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full Phase 7 verification sweep across all seven binaries</name>
  <files>crates/prompter/tests/agent_help.rs, crates/unvenv/tests/agent_help.rs, crates/asana-cli/tests/agent_help.rs, crates/todoer/tests/agent_help.rs, crates/silent-critic/tests/agent_help.rs, crates/gator/tests/agent_help.rs, crates/bsky-comment-extractor/tests/agent_help.rs</files>
  <read_first>.planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-VALIDATION.md, crates/prompter/tests/agent_help.rs, crates/unvenv/tests/agent_help.rs, crates/asana-cli/tests/agent_help.rs, crates/todoer/tests/agent_help.rs, crates/silent-critic/tests/agent_help.rs, crates/gator/tests/agent_help.rs, crates/bsky-comment-extractor/tests/agent_help.rs</read_first>
  <acceptance_criteria>
    - `cargo test --workspace agent_help` passes.
    - `cargo test --workspace --verbose` passes after the targeted sweep.
    - Every listed `tests/agent_help.rs` file exists and contains assertions for top-level success, hidden-help behavior, and subcommand-placement rejection.
  </acceptance_criteria>
  <action>Use the new per-crate `agent_help` test files as the Phase 7 regression surface. Tighten any missing assertions so each of the seven binaries proves top-level `--agent-help` and `--agent-skill` success, normal-help hiding, and subcommand-placement rejection per D-06, D-07, D-08, and D-09. Then run the workspace-targeted agent-doc test sweep followed by the full workspace suite to confirm the rollout did not change unrelated behavior.</action>
  <verify>
    <automated>cargo test --workspace agent_help && cargo test --workspace --verbose</automated>
  </verify>
  <done>All seven binaries are covered by automated Phase 7 regression tests, and the workspace test suite stays green after the rollout.</done>
</task>

</tasks>

<verification>
Run `cargo test --workspace agent_help && cargo test --workspace --verbose`. If either command fails, fix the relevant crate-level assertions before marking the phase ready for execution completion.
</verification>

<success_criteria>
All seven workspace binaries now satisfy the Phase 7 contract: shared-source YAML help, shared-source Claude skill output, hidden top-level-only flags, exhaustive authored docs, and workspace-wide regression coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-04-SUMMARY.md`
</output>
