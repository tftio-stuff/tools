---
phase: 07-add-shared-agent-help-and-agent-skill-support-across-cli-crates
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - crates/bsky-comment-extractor/src/cli.rs
  - crates/bsky-comment-extractor/src/main.rs
  - crates/bsky-comment-extractor/tests/agent_help.rs
  - crates/unvenv/src/main.rs
  - crates/unvenv/tests/agent_help.rs
  - crates/gator/src/cli.rs
  - crates/gator/src/main.rs
  - crates/gator/tests/agent_help.rs
autonomous: true
requirements:
  - ADOC-02
  - ADOC-03
  - ADOC-04
  - ADOC-05
must_haves:
  truths:
    - "`bce`, `unvenv`, and `gator` print exhaustive, crate-specific agent docs at the top level and still preserve their normal CLI behavior."
    - "`tool subcommand --agent-help` fails or falls through to normal clap validation instead of succeeding."
    - "Normal human-facing `--help` output does not expose `--agent-help` or `--agent-skill`."
  artifacts:
    - path: "crates/bsky-comment-extractor/tests/agent_help.rs"
      provides: "Regression coverage for the existing hidden flag implementation and new skill output."
    - path: "crates/unvenv/tests/agent_help.rs"
      provides: "Top-level-only and hidden-help subprocess coverage for the single-file binary."
    - path: "crates/gator/tests/agent_help.rs"
      provides: "Required-positional bypass coverage for agent-doc invocations."
  key_links:
    - from: "crates/bsky-comment-extractor/src/main.rs"
      to: "tftio_cli_common::agent_docs"
      via: "early argv interception before clap dispatch"
      pattern: "detect_agent_doc_request|render_agent_help_yaml|render_agent_skill"
    - from: "crates/unvenv/src/main.rs"
      to: "tftio_cli_common::agent_docs"
      via: "top-level request short-circuit"
      pattern: "detect_agent_doc_request|print_agent"
    - from: "crates/gator/src/main.rs"
      to: "tftio_cli_common::agent_docs"
      via: "pre-validate doc dispatch before required positional parsing"
      pattern: "detect_agent_doc_request|Cli::parse"
---

<objective>
Roll out the shared agent-doc contract to the three crates with the clearest top-level entrypoints and existing CLI regression surface.

Purpose: Prove the shared API works for an existing agent-help crate (`bce`), a single-file binary (`unvenv`), and a required-positional CLI (`gator`).
Output: Three binaries with exhaustive per-tool docs, hidden top-level flags, and subprocess coverage.
</objective>

<execution_context>
@/Users/jfb/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jfb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-CONTEXT.md
@.planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-RESEARCH.md
@.planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-VALIDATION.md
@CLAUDE.md
@crates/bsky-comment-extractor/src/cli.rs
@crates/bsky-comment-extractor/src/main.rs
@crates/unvenv/src/main.rs
@crates/gator/src/cli.rs
@crates/gator/src/main.rs
@crates/cli-common/src/agent_docs.rs

<interfaces>
Existing `bce` parser:
```rust
pub struct Cli {
    pub agent_help: bool,
    pub command: Option<Command>,
}
```

Existing `gator` parser requires a positional agent:
```rust
pub struct Cli {
    pub agent: Agent,
    pub profiles: Vec<String>,
    pub json: bool,
    pub agent_args: Vec<String>,
}
```

Shared API from Plan 07-01:
```rust
pub enum AgentDocRequest { Help, Skill }
pub fn detect_agent_doc_request<I, S>(args: I) -> Option<AgentDocRequest>;
pub fn render_agent_help_yaml(doc: &AgentDoc) -> String;
pub fn render_agent_skill(doc: &AgentDoc) -> String;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace bce's placeholder help with shared agent-doc wiring and add unvenv support</name>
  <files>crates/bsky-comment-extractor/src/cli.rs, crates/bsky-comment-extractor/src/main.rs, crates/bsky-comment-extractor/tests/agent_help.rs, crates/unvenv/src/main.rs, crates/unvenv/tests/agent_help.rs</files>
  <read_first>.planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-CONTEXT.md, .planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-RESEARCH.md, crates/cli-common/src/agent_docs.rs, crates/bsky-comment-extractor/src/cli.rs, crates/bsky-comment-extractor/src/main.rs, crates/unvenv/src/main.rs</read_first>
  <acceptance_criteria>
    - `rg -n "agent_skill|--agent-skill" crates/bsky-comment-extractor/src/cli.rs crates/unvenv/src/main.rs` shows both flags are declared or intercepted per crate.
    - `rg -n "pending_phase_06" crates/bsky-comment-extractor/src/main.rs` returns no matches.
    - `cargo test -p tftio-bsky-comment-extractor agent_help` passes, including success for `bce --agent-help` and failure or normal clap error for `bce query --agent-help`.
    - `cargo test -p tftio-unvenv agent_help` passes, including success for top-level `--agent-help` and `--agent-skill`, plus hidden-help assertions.
  </acceptance_criteria>
  <behavior>
    - Test 1: `bce --agent-help` prints canonical YAML with fetch/query docs, env vars, database paths, query envelope shape, and failure guidance; `bce --agent-skill` prints the same facts as a Claude-style skill doc.
    - Test 2: `unvenv --agent-help` and `unvenv --agent-skill` succeed without requiring `scan`, while `unvenv scan --agent-help` does not trigger agent-doc output.
    - Test 3: `bce --help` and `unvenv --help` omit both hidden flags.
  </behavior>
  <action>For `bce`, remove the existing `global = true` hidden flag behavior that currently allows subcommand placement, and replace it with raw-argv interception plus a per-crate `AgentDoc` builder that documents `fetch`, `query`, `BSKY_APP_PASSWORD`, XDG database defaults, JSONL envelope output, known error codes, and common operator mistakes per D-02, D-03, D-04, D-05, D-07, and D-08. Keep normal `fetch` and `query` behavior unchanged when no top-level agent-doc request is present. For `unvenv`, add the same top-level interception path and author exhaustive docs for `scan`, `doctor`, `completions`, `update`, exit code `2` on detected virtual environments, repository-discovery behavior, and likely operator mistakes. Add subprocess tests in each crate's `tests/agent_help.rs` file that assert stdout output, exit code `0`, hidden help, and top-level-only enforcement.</action>
  <verify>
    <automated>cargo test -p tftio-bsky-comment-extractor agent_help && cargo test -p tftio-unvenv agent_help</automated>
  </verify>
  <done>`bce` and `unvenv` expose exhaustive top-level agent docs in both formats without leaking the flags into normal help or subcommand contexts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add shared agent-doc output to gator without weakening required positional parsing</name>
  <files>crates/gator/src/cli.rs, crates/gator/src/main.rs, crates/gator/tests/agent_help.rs</files>
  <read_first>.planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-CONTEXT.md, .planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-RESEARCH.md, crates/cli-common/src/agent_docs.rs, crates/gator/src/cli.rs, crates/gator/src/main.rs</read_first>
  <acceptance_criteria>
    - `rg -n "agent_skill|--agent-skill|detect_agent_doc_request" crates/gator/src/main.rs crates/gator/src/cli.rs` shows the top-level interception path and hidden flag support.
    - `cargo test -p tftio-gator agent_help` passes, including success for `gator --agent-help` despite the normal required `agent` positional.
    - `cargo test -p tftio-gator agent_help` also verifies `gator claude --agent-help` does not return agent-doc output and `gator --help` omits both hidden flags.
  </acceptance_criteria>
  <behavior>
    - Test 1: top-level `gator --agent-help` and `gator --agent-skill` bypass required positional parsing and exit `0`.
    - Test 2: agent docs enumerate agent targets, profile behavior, session/policy constraints, JSON error mode, and likely operator mistakes.
    - Test 3: normal clap validation for `gator claude --session ...` conflicts remains unchanged when agent-doc flags are absent.
  </behavior>
  <action>Integrate the shared top-level detector into `crates/gator/src/main.rs` before `Cli::parse()` so the new agent-doc requests do not require the `agent` positional or alter `Cli::validate()` semantics. Author a `gator`-specific `AgentDoc` that covers supported agents, policy/profile arguments, session-mode incompatibilities, YOLO injection behavior, dry-run mode, JSON error output, and the macOS sandbox requirement per D-02, D-03, D-07, and D-09. Keep the hidden flags out of normal help output, and add a new subprocess-style integration test file under `crates/gator/tests/agent_help.rs` instead of relying only on unit tests.</action>
  <verify>
    <automated>cargo test -p tftio-gator agent_help</automated>
  </verify>
  <done>`gator` can emit exhaustive agent docs from the top level without changing its normal required-positional and validation behavior.</done>
</task>

</tasks>

<verification>
After both tasks, run `cargo test -p tftio-bsky-comment-extractor agent_help && cargo test -p tftio-unvenv agent_help && cargo test -p tftio-gator agent_help` and spot-check that all three binaries keep `--agent-help` / `--agent-skill` hidden from human help output.
</verification>

<success_criteria>
Three crates of different parser shapes now share the Phase 7 behavior: exhaustive per-tool docs, top-level-only requests, hidden flags, and no regressions in normal command parsing.
</success_criteria>

<output>
After completion, create `.planning/phases/07-add-shared-agent-help-and-agent-skill-support-across-cli-crates/07-02-SUMMARY.md`
</output>
