---
phase: 01-cli-common-unification
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - crates/gator/src/cli.rs
  - crates/gator/src/main.rs
  - crates/gator/src/lib.rs
  - crates/todoer/src/cli.rs
  - crates/todoer/src/main.rs
  - crates/todoer/src/output.rs
  - crates/silent-critic/src/cli.rs
  - crates/silent-critic/src/main.rs
  - crates/silent-critic/src/output.rs
autonomous: true
requirements:
  - CLI-UNIFY-02
must_haves:
  truths:
    - "gator, todoer, and silent-critic use one shared JSON envelope implementation from cli-common"
    - "gator, todoer, and silent-critic use one shared top-level error printer from cli-common"
    - "all three crates expose the same base metadata-command surface where supported by their clap trees"
  artifacts:
    - path: "crates/todoer/src/main.rs"
      provides: "todoer top-level error and response wiring via cli-common"
      contains: "tftio_cli_common"
    - path: "crates/silent-critic/src/main.rs"
      provides: "silent-critic top-level error and response wiring via cli-common"
      contains: "tftio_cli_common"
    - path: "crates/gator/src/main.rs"
      provides: "gator top-level JSON/plain-text error routing via cli-common"
      contains: "print_error"
  key_links:
    - from: "crates/todoer/src/main.rs"
      to: "crates/cli-common/src/json.rs"
      via: "ok_response / err_response imports"
      pattern: "ok_response|err_response"
    - from: "crates/silent-critic/src/main.rs"
      to: "crates/cli-common/src/error.rs"
      via: "shared error printing"
      pattern: "print_error"
---

<objective>
Migrate the machine-oriented CLIs to the shared `cli-common` surface so JSON mode, text errors, and metadata commands are identical across `gator`, `todoer`, and `silent-critic`.

Purpose: Remove the most obvious duplicated agent-facing UX first.
Output: Three consumer crates rewritten to use `cli-common`'s JSON, error, and metadata-command helpers.
</objective>

<execution_context>
@/Users/jfb/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jfb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-i-want-you-to-examine-all-the-code-here-and-extract-into-the-cli-common-create-all-common-functionality-be-extremely-thorough-i-want-these-tools-to-have-a-completely-uniform-base-ui-ux-and-i-want-all-dependencies-to-be-centralized/01-RESEARCH.md
@.planning/phases/01-i-want-you-to-examine-all-the-code-here-and-extract-into-the-cli-common-create-all-common-functionality-be-extremely-thorough-i-want-these-tools-to-have-a-completely-uniform-base-ui-ux-and-i-want-all-dependencies-to-be-centralized/01-01-PLAN.md

<interfaces>
Current duplicate JSON envelope modules:
- `crates/todoer/src/output.rs`
- `crates/silent-critic/src/output.rs`

Current plain/JSON error split:
- `crates/gator/src/main.rs`
- `crates/todoer/src/main.rs`
- `crates/silent-critic/src/main.rs`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace duplicated JSON envelope helpers in todoer and silent-critic</name>
  <files>crates/todoer/src/main.rs, crates/todoer/src/output.rs, crates/silent-critic/src/main.rs, crates/silent-critic/src/output.rs</files>
  <read_first>
    - crates/todoer/src/main.rs
    - crates/todoer/src/output.rs
    - crates/silent-critic/src/main.rs
    - crates/silent-critic/src/output.rs
    - crates/cli-common/src/json.rs
    - crates/cli-common/src/error.rs
  </read_first>
  <action>
Use the exact shared helpers from `cli-common` and delete the duplicated local envelope logic:

1. In `crates/todoer/src/main.rs`, replace imports from `todoer::output::{err_response, ok_response, render_task_table}` with:
   - `use tftio_cli_common::json::{err_response, ok_response};`
   - keep `render_task_table` local by either moving just that function into `main.rs` or creating a narrowed local module containing only table rendering.

2. In `crates/silent-critic/src/main.rs`, replace imports from `silent_critic::output::{err_response, ok_response}` with `tftio_cli_common::json::{err_response, ok_response}`.

3. Delete the duplicated `ok_response` and `err_response` function bodies from `crates/todoer/src/output.rs` and `crates/silent-critic/src/output.rs`. If a file becomes unnecessary, remove the module entirely and update `lib.rs` exports. If a file still contains crate-specific helpers, leave only the crate-specific helpers.

4. Replace each crate's top-level error branch with `tftio_cli_common::error::print_error(command_name, json, &error.to_string())` for the fallback path. Use exact command names already visible in each match arm, such as `"list"`, `"task.status"`, and `"error"`.

5. Keep the existing JSON payload content identical apart from originating from `cli-common`.
  </action>
  <verify>
    <automated>cargo test -p tftio-todoer && cargo test -p tftio-silent-critic</automated>
  </verify>
  <acceptance_criteria>
    - `crates/todoer/src/main.rs` imports `tftio_cli_common::json::ok_response`
    - `crates/todoer/src/main.rs` imports `tftio_cli_common::json::err_response`
    - `crates/silent-critic/src/main.rs` imports `tftio_cli_common::json::ok_response`
    - `crates/silent-critic/src/main.rs` imports `tftio_cli_common::json::err_response`
    - `crates/todoer/src/output.rs` no longer defines `pub fn ok_response`
    - `crates/silent-critic/src/output.rs` no longer defines `pub fn err_response`
    - `cargo test -p tftio-todoer` exits 0
    - `cargo test -p tftio-silent-critic` exits 0
  </acceptance_criteria>
  <done>
    Both crates consume one shared JSON envelope implementation and keep only genuinely crate-specific output helpers.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize base metadata commands and top-level errors in gator, todoer, and silent-critic</name>
  <files>crates/gator/src/cli.rs, crates/gator/src/main.rs, crates/gator/src/lib.rs, crates/todoer/src/cli.rs, crates/todoer/src/main.rs, crates/silent-critic/src/cli.rs, crates/silent-critic/src/main.rs</files>
  <read_first>
    - crates/gator/src/cli.rs
    - crates/gator/src/main.rs
    - crates/gator/src/lib.rs
    - crates/todoer/src/cli.rs
    - crates/todoer/src/main.rs
    - crates/silent-critic/src/cli.rs
    - crates/silent-critic/src/main.rs
    - crates/cli-common/src/app.rs
    - crates/cli-common/src/command.rs
    - crates/cli-common/src/error.rs
  </read_first>
  <action>
Add one uniform metadata-command layer to the three machine-oriented CLIs using the exact `cli-common` APIs from Plan 01:

1. In each crate, create a `ToolSpec` constant or function with exact values:
   - `gator`: `bin_name="gator"`, `display_name="Gator"`, `license=LicenseType::MIT`, `repo=RepoInfo::new("tftio-stuff", "tools")`, `supports_json=true`, `supports_doctor=false`, `supports_update=false`
   - `todoer`: `bin_name="todoer"`, `display_name="Todoer"`, `license=LicenseType::CC0`, `repo=RepoInfo::new("tftio-stuff", "tools")`, `supports_json=true`, `supports_doctor=false`, `supports_update=false`
   - `silent-critic`: `bin_name="silent-critic"`, `display_name="Silent Critic"`, `license=LicenseType::MIT`, `repo=RepoInfo::new("tftio-stuff", "tools")`, `supports_json=true`, `supports_doctor=false`, `supports_update=false`

2. Extend each clap command tree with a `Meta` or equivalent subcommand that routes to `StandardCommand` variants for `Version`, `License`, and `Completions`. Reuse each crate's existing `--json` support for `Version { json: true }`.

3. In each `main.rs`, route failures through `print_error(...)` instead of hand-rolled `eprintln!` / raw JSON string literals.

4. Preserve all existing domain command names and business logic. Only the base UX and top-level error handling should change.

5. Add parse tests asserting the new metadata command path exists in all three crates, and update existing JSON output tests to confirm the envelope shape is unchanged.
  </action>
  <verify>
    <automated>cargo test -p tftio-gator && cargo test -p tftio-todoer && cargo test -p tftio-silent-critic</automated>
  </verify>
  <acceptance_criteria>
    - `crates/gator/src/cli.rs` contains `Version`
    - `crates/todoer/src/cli.rs` contains `Version`
    - `crates/silent-critic/src/cli.rs` contains `Version`
    - `crates/gator/src/main.rs` contains `print_error(`
    - `crates/todoer/src/main.rs` contains `print_error(`
    - `crates/silent-critic/src/main.rs` contains `print_error(`
    - `cargo test -p tftio-gator` exits 0
    - `cargo test -p tftio-todoer` exits 0
    - `cargo test -p tftio-silent-critic` exits 0
  </acceptance_criteria>
  <done>
    The machine-oriented CLIs share one top-level UX contract for metadata commands and error rendering.
  </done>
</task>

</tasks>

<verification>
- `cargo test -p tftio-gator`
- `cargo test -p tftio-todoer`
- `cargo test -p tftio-silent-critic`
- `cargo clippy -p tftio-gator -p tftio-todoer -p tftio-silent-critic -- -D warnings`
</verification>

<success_criteria>
- JSON envelope duplication removed
- Top-level machine-readable UX is consistent across three agent-facing CLIs
- No crate-specific raw JSON string construction remains in entrypoints
</success_criteria>

<output>
After completion, create `.planning/phases/01-i-want-you-to-examine-all-the-code-here-and-extract-into-the-cli-common-create-all-common-functionality-be-extremely-thorough-i-want-these-tools-to-have-a-completely-uniform-base-ui-ux-and-i-want-all-dependencies-to-be-centralized/01-02-SUMMARY.md`
</output>
