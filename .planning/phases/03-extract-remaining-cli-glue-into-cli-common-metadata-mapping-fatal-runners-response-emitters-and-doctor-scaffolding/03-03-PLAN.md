---
phase: 03-extract-remaining-cli-glue-into-cli-common-metadata-mapping-fatal-runners-response-emitters-and-doctor-scaffolding
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - crates/silent-critic/src/main.rs
  - crates/unvenv/src/main.rs
  - crates/asana-cli/src/main.rs
  - crates/asana-cli/src/cli/mod.rs
  - crates/prompter/src/doctor.rs
  - crates/prompter/src/lib.rs
autonomous: true
requirements:
  - CLI-SHARE-05
  - CLI-SHARE-06
must_haves:
  truths:
    - "the richer remaining tools stop hand-rolling JSON/text success branching, fatal runners, or doctor provider scaffolding when `cli-common` can express them"
    - "`silent-critic` keeps domain summaries local while deleting reusable response boilerplate"
    - "`unvenv`, `asana-cli`, and `prompter` use shared fatal/doctor helpers without losing their tool-specific behavior"
  artifacts:
    - path: "crates/silent-critic/src/main.rs"
      provides: "silent-critic using shared response emission for more command paths"
      contains: "render_response|ok_response"
    - path: "crates/unvenv/src/main.rs"
      provides: "unvenv using shared runner or fatal helpers"
      contains: "tftio_cli_common"
    - path: "crates/asana-cli/src/main.rs"
      provides: "asana-cli top-level error path using shared runner/error surface"
      contains: "print_error|tftio_cli_common"
    - path: "crates/prompter/src/doctor.rs"
      provides: "prompter doctor built from shared provider/report helpers"
      contains: "DoctorReport|DoctorCheck"
  key_links:
    - from: "crates/silent-critic/src/main.rs"
      to: "crates/cli-common/src/json.rs"
      via: "shared JSON/text response emission"
      pattern: "render_response|ok_response"
    - from: "crates/prompter/src/doctor.rs"
      to: "crates/cli-common/src/doctor.rs"
      via: "shared doctor provider scaffolding"
      pattern: "DoctorReport|DoctorCheck"
---

<objective>
Migrate the richer remaining tools onto the final `cli-common` helper surface.

Purpose: remove the last repeated success-path, fatal-runner, and doctor-provider glue while preserving tool-specific behavior.
Output: `silent-critic`, `unvenv`, `asana-cli`, and `prompter` keep domain logic but stop owning reusable CLI plumbing.
</objective>

<execution_context>
@/Users/jfb/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jfb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-extract-remaining-cli-glue-into-cli-common-metadata-mapping-fatal-runners-response-emitters-and-doctor-scaffolding/03-RESEARCH.md
@.planning/phases/03-extract-remaining-cli-glue-into-cli-common-metadata-mapping-fatal-runners-response-emitters-and-doctor-scaffolding/03-01-PLAN.md
@.planning/phases/03-extract-remaining-cli-glue-into-cli-common-metadata-mapping-fatal-runners-response-emitters-and-doctor-scaffolding/03-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace reusable success-path branching in `silent-critic`</name>
  <files>crates/silent-critic/src/main.rs, crates/cli-common/src/json.rs</files>
  <read_first>
    - crates/silent-critic/src/main.rs
    - crates/cli-common/src/json.rs
    - crates/cli-common/src/output.rs
  </read_first>
  <action>
Use the richer shared response helper to delete the remaining large JSON/text branching surface in `silent-critic`:

1. Replace repeated `if json { ... } else { ... }` success blocks with the shared response emitter where the abstraction stays clearer than the duplicated branches.
2. Keep criterion/session/project text summaries local to `silent-critic`.
3. Extend the shared response helper only when the new shape is clearly reusable outside `silent-critic`.
  </action>
  <verify>
    <automated>cargo test -p tftio-silent-critic && cargo clippy -p tftio-silent-critic -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - the remaining reusable success-path branching is materially reduced in `silent-critic`
    - domain-specific summary formatting remains local
    - targeted tests and clippy checks exit 0
  </acceptance_criteria>
  <done>
    `silent-critic` keeps domain rendering but uses shared response plumbing for the rest.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace remaining fatal-runner and doctor-provider glue in `unvenv`, `asana-cli`, and `prompter`</name>
  <files>crates/unvenv/src/main.rs, crates/asana-cli/src/main.rs, crates/asana-cli/src/cli/mod.rs, crates/prompter/src/doctor.rs, crates/prompter/src/lib.rs</files>
  <read_first>
    - crates/unvenv/src/main.rs
    - crates/asana-cli/src/main.rs
    - crates/asana-cli/src/cli/mod.rs
    - crates/prompter/src/doctor.rs
    - crates/prompter/src/lib.rs
    - crates/cli-common/src/runner.rs
    - crates/cli-common/src/doctor.rs
    - crates/cli-common/src/error.rs
  </read_first>
  <action>
Delete the remaining reusable runner and doctor scaffolding from the richer tools:

1. Refactor `unvenv` and `asana-cli` onto the shared runner/fatal helper surface where it simplifies their top-level entrypoints.
2. Refactor `prompter` and any remaining tool doctor providers to use the shared doctor builder/helper instead of repeating repo/version/detail/warning/error scaffolding.
3. Preserve `unvenv` scan behavior, `asana-cli` tracing and domain dispatch, and `prompter` config/state collection logic.
  </action>
  <verify>
    <automated>cargo test -p tftio-unvenv -p tftio-asana-cli -p tftio-prompter && cargo clippy -p tftio-unvenv -p tftio-asana-cli -p tftio-prompter -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - shared runner/error helpers replace remaining repeated top-level fatal plumbing where practical
    - shared doctor-provider helpers replace repeated repo/version/detail scaffolding where practical
    - targeted tests for `unvenv`, `asana-cli`, and `prompter` remain green
  </acceptance_criteria>
  <done>
    The richer remaining tools now keep only genuinely tool-specific logic on top of the shared CLI substrate.
  </done>
</task>
