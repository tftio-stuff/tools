---
phase: 05-query-subcommand
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - crates/bsky-comment-extractor/src/cli.rs
autonomous: true
requirements: [QUERY-02, QUERY-03, QUERY-04]
---

<objective>
Migrate BCE to explicit `fetch` and `query` subcommands with locked query pagination flags and a reserved top-level `--agent-help` flag.
</objective>

<tasks>
- Replace the flat parser with top-level `fetch` and `query` subcommands.
- Keep query parsing limited to `--db`, `--limit`, and `--offset` with defaults 50 and 0.
- Add parser tests for subcommand routing, defaults, and top-level `--agent-help` parsing.
</tasks>

<output>
Create `.planning/phases/05-query-subcommand/05-02-SUMMARY.md`.
</output>
