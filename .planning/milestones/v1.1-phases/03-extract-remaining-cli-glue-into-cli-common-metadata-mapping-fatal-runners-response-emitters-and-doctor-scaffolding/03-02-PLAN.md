---
phase: 03-extract-remaining-cli-glue-into-cli-common-metadata-mapping-fatal-runners-response-emitters-and-doctor-scaffolding
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - crates/gator/src/main.rs
  - crates/bsky-comment-extractor/src/main.rs
  - crates/todoer/src/main.rs
autonomous: true
requirements:
  - CLI-SHARE-05
  - CLI-SHARE-06
must_haves:
  truths:
    - "the thin-entrypoint tools delete their remaining local metadata-mapping, fatal-runner, and wrapper glue in favor of the new `cli-common` helpers"
    - "`gator`, `bce`, and `todoer` preserve their existing invocation syntax while getting thinner entrypoints"
    - "`todoer` drops leftover zero-value wrappers and uses the richer shared response surface where practical"
  artifacts:
    - path: "crates/gator/src/main.rs"
      provides: "gator entrypoint using shared metadata and runner helpers"
      contains: "tftio_cli_common"
    - path: "crates/bsky-comment-extractor/src/main.rs"
      provides: "bce entrypoint using shared metadata/doctor helpers"
      contains: "tftio_cli_common"
    - path: "crates/todoer/src/main.rs"
      provides: "todoer command flow with reduced response/error boilerplate"
      contains: "render_response|tftio_cli_common"
  key_links:
    - from: "crates/gator/src/main.rs"
      to: "crates/cli-common/src/command.rs"
      via: "shared metadata-command mapping"
      pattern: "StandardCommand|MetaCommand"
    - from: "crates/bsky-comment-extractor/src/main.rs"
      to: "crates/cli-common/src/doctor.rs"
      via: "shared doctor provider scaffolding"
      pattern: "Doctor|tool_checks"
---

<objective>
Migrate the thinner entrypoint tools onto the final `cli-common` helper surface.

Purpose: delete the remaining low-risk metadata, runner, response, and doctor wrapper code from `gator`, `bce`, and `todoer`.
Output: these tools keep only domain behavior plus the shared CLI substrate.
</objective>

<execution_context>
@/Users/jfb/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jfb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-extract-remaining-cli-glue-into-cli-common-metadata-mapping-fatal-runners-response-emitters-and-doctor-scaffolding/03-RESEARCH.md
@.planning/phases/03-extract-remaining-cli-glue-into-cli-common-metadata-mapping-fatal-runners-response-emitters-and-doctor-scaffolding/03-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove remaining metadata and doctor scaffolding from `gator` and `bce`</name>
  <files>crates/gator/src/main.rs, crates/bsky-comment-extractor/src/main.rs</files>
  <read_first>
    - crates/gator/src/main.rs
    - crates/bsky-comment-extractor/src/main.rs
    - crates/cli-common/src/command.rs
    - crates/cli-common/src/doctor.rs
    - crates/cli-common/src/runner.rs
  </read_first>
  <action>
Apply the new `cli-common` helpers to the remaining thin entrypoints:

1. Replace local metadata enum-to-`StandardCommand` matches with the shared helper surface where the clap tree allows it.
2. Replace repeated run/fatal-error/exit code plumbing with the shared runner helper where it keeps the entrypoint simpler.
3. Move repeated doctor provider scaffolding out of `bce` if the new helper can express it cleanly.
4. Keep command syntax and extraction behavior unchanged.
  </action>
  <verify>
    <automated>cargo test -p tftio-gator -p tftio-bsky-comment-extractor && cargo clippy -p tftio-gator -p tftio-bsky-comment-extractor -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `gator` and `bce` no longer keep removable local metadata-mapping glue that `cli-common` can provide
    - `bce` no longer repeats doctor provider scaffolding that the shared helper can express
    - both crates preserve their current command syntax and pass targeted tests
  </acceptance_criteria>
  <done>
    The thinner entrypoints now consume the final shared metadata/runner/doctor surface.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove leftover wrapper and response boilerplate from `todoer`</name>
  <files>crates/todoer/src/main.rs</files>
  <read_first>
    - crates/todoer/src/main.rs
    - crates/cli-common/src/json.rs
    - crates/cli-common/src/error.rs
    - crates/cli-common/src/output.rs
  </read_first>
  <action>
Use the richer shared response/error helpers in `todoer`:

1. Delete any zero-value local wrappers around `cli-common` helpers.
2. Replace any remaining repeated JSON/text success branching with the new shared response emitter where it improves clarity.
3. Keep task tables, notes, and task-detail formatting local to `todoer`.
  </action>
  <verify>
    <automated>cargo test -p tftio-todoer && cargo clippy -p tftio-todoer -- -D warnings && just cli-metadata-consistency</automated>
  </verify>
  <acceptance_criteria>
    - `todoer` no longer contains zero-value wrappers around `cli-common`
    - remaining reusable response glue is deleted where the new shared helper can express it cleanly
    - `todoer` tests and the metadata shell suite remain green
  </acceptance_criteria>
  <done>
    `todoer` keeps only domain-specific command text on top of the shared CLI response surface.
  </done>
</task>
