---
phase: 03-extract-remaining-cli-glue-into-cli-common-metadata-mapping-fatal-runners-response-emitters-and-doctor-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cli-common/Cargo.toml
  - crates/cli-common/src/lib.rs
  - crates/cli-common/src/command.rs
  - crates/cli-common/src/doctor.rs
  - crates/cli-common/src/error.rs
  - crates/cli-common/src/json.rs
  - crates/cli-common/src/output.rs
  - crates/cli-common/src/runner.rs
autonomous: true
requirements:
  - CLI-SHARE-05
  - CLI-SHARE-06
must_haves:
  truths:
    - "`cli-common` exposes the final shared helper layer for metadata mapping, fatal runners, response emission, and doctor provider scaffolding"
    - "the new shared helpers are test-covered before any consumer migration depends on them"
    - "the added surface removes repeated CLI glue without absorbing domain behavior"
  artifacts:
    - path: "crates/cli-common/src/command.rs"
      provides: "shared metadata-command adapter helpers"
      contains: "StandardCommand|meta"
    - path: "crates/cli-common/src/runner.rs"
      provides: "shared top-level runner / fatal error helper"
      contains: "run_|fatal"
    - path: "crates/cli-common/src/json.rs"
      provides: "richer shared response emitter"
      contains: "render_response|ok_response"
    - path: "crates/cli-common/src/doctor.rs"
      provides: "shared doctor provider/build scaffolding"
      contains: "DoctorReport|DoctorCheck"
  key_links:
    - from: "crates/cli-common/src/command.rs"
      to: "crates/gator/src/main.rs"
      via: "shared metadata enum mapping"
      pattern: "StandardCommand|MetaCommand"
    - from: "crates/cli-common/src/runner.rs"
      to: "crates/unvenv/src/main.rs"
      via: "shared parse/run/error/exit helper"
      pattern: "print_error|exit"
---

<objective>
Add the final reusable helper layer to `tftio-cli-common` so the remaining entrypoint, error, response, and doctor glue can disappear from tool crates.

Purpose: make the final migration plans delete local glue instead of moving it around.
Output: new shared metadata-mapping helpers, runner helpers, richer response emitters, and doctor-provider scaffolding in `cli-common`.
</objective>

<execution_context>
@/Users/jfb/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jfb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-extract-remaining-cli-glue-into-cli-common-metadata-mapping-fatal-runners-response-emitters-and-doctor-scaffolding/03-RESEARCH.md
@.planning/phases/03-extract-remaining-cli-glue-into-cli-common-metadata-mapping-fatal-runners-response-emitters-and-doctor-scaffolding/03-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared metadata-mapping and runner helpers to `cli-common`</name>
  <files>crates/cli-common/src/command.rs, crates/cli-common/src/error.rs, crates/cli-common/src/runner.rs, crates/cli-common/src/lib.rs</files>
  <read_first>
    - crates/cli-common/src/command.rs
    - crates/cli-common/src/error.rs
    - crates/gator/src/main.rs
    - crates/bsky-comment-extractor/src/main.rs
    - crates/unvenv/src/main.rs
    - crates/asana-cli/src/main.rs
  </read_first>
  <action>
Create the final shared entrypoint layer needed to delete the remaining metadata and fatal-runner glue:

1. Add helper APIs for mapping crate-local metadata enums into `StandardCommand` without repeated per-tool `match` boilerplate.
2. Add a shared runner helper that centralizes parse/run/fatal-error/exit handling where tool command shapes allow it.
3. Keep the helper surface small and explicit; do not force every tool into the same clap layout.
4. Add focused tests covering the new helper behavior before any tool migration uses it.
  </action>
  <verify>
    <automated>cargo test -p tftio-cli-common && cargo clippy -p tftio-cli-common -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `cli-common` exports metadata-mapping helpers that can replace repeated local enum-to-`StandardCommand` code
    - `cli-common` exports a shared runner or fatal helper that can replace repeated top-level error plumbing in multiple tools
    - focused tests cover the new shared helper behavior
    - `cargo test -p tftio-cli-common` exits 0
    - `cargo clippy -p tftio-cli-common -- -D warnings` exits 0
  </acceptance_criteria>
  <done>
    The final entrypoint glue layer exists in `cli-common`, ready for consumer migrations.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add richer shared response emission and doctor-provider scaffolding</name>
  <files>crates/cli-common/src/json.rs, crates/cli-common/src/output.rs, crates/cli-common/src/doctor.rs, crates/cli-common/src/lib.rs</files>
  <read_first>
    - crates/cli-common/src/json.rs
    - crates/cli-common/src/output.rs
    - crates/cli-common/src/doctor.rs
    - crates/silent-critic/src/main.rs
    - crates/todoer/src/main.rs
    - crates/prompter/src/doctor.rs
    - crates/unvenv/src/main.rs
  </read_first>
  <action>
Expand `cli-common` so the remaining response/doctor glue stops living in the tools:

1. Add a shared command-output helper that removes repeated `if json { ... } else { ... }` success branching while preserving per-command text formatting.
2. Add a small doctor provider/builder helper for repo/version/check/detail wiring so tools stop repeating the same scaffolding around the shared doctor renderer.
3. Avoid extracting task tables, extraction summaries, or other domain-specific rendering.
4. Add focused tests for the new shared response and doctor helper surfaces.
  </action>
  <verify>
    <automated>cargo test -p tftio-cli-common && cargo clippy -p tftio-cli-common -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `cli-common` provides a richer response emitter reusable by multiple tools
    - `cli-common` provides doctor-provider/build helpers that can replace repeated repo/version/check wiring
    - focused tests cover the new response and doctor helper surfaces
    - targeted `cargo test` and `cargo clippy` exit 0
  </acceptance_criteria>
  <done>
    `cli-common` now owns the last broadly useful response and doctor scaffolding needed for the remaining migrations.
  </done>
</task>
