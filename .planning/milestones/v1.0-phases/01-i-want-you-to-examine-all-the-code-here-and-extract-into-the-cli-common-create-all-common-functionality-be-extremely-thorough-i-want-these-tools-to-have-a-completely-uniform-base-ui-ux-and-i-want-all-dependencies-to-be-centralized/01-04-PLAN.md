---
phase: 01-cli-common-unification
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - crates/prompter/src/lib.rs
  - crates/prompter/src/main.rs
  - crates/prompter/src/doctor.rs
  - crates/prompter/src/completions.rs
  - crates/prompter/tests/cli.rs
  - Cargo.toml
  - CLAUDE.md
  - .planning/PROJECT.md
  - justfile
autonomous: true
requirements:
  - CLI-UNIFY-04
must_haves:
  truths:
    - "prompter adopts the shared base CLI contract without losing dynamic profile completions"
    - "remaining duplicated base-UX logic is removed or documented as intentionally crate-specific"
    - "workspace verification covers all binary crates for help, metadata commands, and JSON/text error expectations"
  artifacts:
    - path: "crates/prompter/src/main.rs"
      provides: "prompter entrypoint using cli-common base contract"
      contains: "StandardCommand"
    - path: "crates/prompter/src/completions.rs"
      provides: "dynamic profile completion adapter over the shared completion wrapper"
      contains: "augment_"
    - path: "justfile"
      provides: "workspace-level CLI consistency target"
      contains: "cli-consistency"
  key_links:
    - from: "crates/prompter/src/completions.rs"
      to: "crates/cli-common/src/completions.rs"
      via: "shared completion wrapper plus prompter-specific augmentation"
      pattern: "generate_completions"
    - from: "justfile"
      to: "crates/*/src/main.rs"
      via: "CLI consistency verification target"
      pattern: "--help"
---

<objective>
Finish the migration by adapting `prompter` to the shared base contract, then add workspace-wide consistency verification and documentation that codifies what belongs in `cli-common` versus what stays local.

Purpose: Close the remaining high-customization gap and prevent the workspace from drifting back into per-crate UX forks.
Output: `prompter` migrated, consistency checks added, and final duplication/audit docs updated.
</objective>

<execution_context>
@/Users/jfb/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jfb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-i-want-you-to-examine-all-the-code-here-and-extract-into-the-cli-common-create-all-common-functionality-be-extremely-thorough-i-want-these-tools-to-have-a-completely-uniform-base-ui-ux-and-i-want-all-dependencies-to-be-centralized/01-RESEARCH.md
@.planning/phases/01-i-want-you-to-examine-all-the-code-here-and-extract-into-the-cli-common-create-all-common-functionality-be-extremely-thorough-i-want-these-tools-to-have-a-completely-uniform-base-ui-ux-and-i-want-all-dependencies-to-be-centralized/01-VALIDATION.md
@.planning/phases/01-i-want-you-to-examine-all-the-code-here-and-extract-into-the-cli-common-create-all-common-functionality-be-extremely-thorough-i-want-these-tools-to-have-a-completely-uniform-base-ui-ux-and-i-want-all-dependencies-to-be-centralized/01-01-PLAN.md
@.planning/phases/01-i-want-you-to-examine-all-the-code-here-and-extract-into-the-cli-common-create-all-common-functionality-be-extremely-thorough-i-want-these-tools-to-have-a-completely-uniform-base-ui-ux-and-i-want-all-dependencies-to-be-centralized/01-02-PLAN.md
@.planning/phases/01-i-want-you-to-examine-all-the-code-here-and-extract-into-the-cli-common-create-all-common-functionality-be-extremely-thorough-i-want-these-tools-to-have-a-completely-uniform-base-ui-ux-and-i-want-all-dependencies-to-be-centralized/01-03-PLAN.md

<interfaces>
`prompter` specifics that must survive migration:
- `AppMode` abstraction in `crates/prompter/src/lib.rs`
- dynamic completion augmentation in `crates/prompter/src/completions.rs`
- doctor behavior in `crates/prompter/src/doctor.rs`

Workspace consistency sources:
- all `crates/*/src/main.rs`
- all `crates/*/tests/cli*.rs`
- `justfile`
- `CLAUDE.md`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adapt prompter to the shared base UX while preserving dynamic completion behavior</name>
  <files>crates/prompter/src/lib.rs, crates/prompter/src/main.rs, crates/prompter/src/doctor.rs, crates/prompter/src/completions.rs, crates/prompter/tests/cli.rs</files>
  <read_first>
    - crates/prompter/src/lib.rs
    - crates/prompter/src/main.rs
    - crates/prompter/src/doctor.rs
    - crates/prompter/src/completions.rs
    - crates/prompter/tests/cli.rs
    - crates/cli-common/src/app.rs
    - crates/cli-common/src/command.rs
    - crates/cli-common/src/completions.rs
    - crates/cli-common/src/doctor.rs
  </read_first>
  <action>
Preserve `prompter`'s custom logic, but move the base contract to `cli-common`:

1. Add a `ToolSpec` for `prompter` with exact values `bin_name="prompter"`, `display_name="Prompter"`, `license=LicenseType::MIT`, `repo=RepoInfo::new("tftio-stuff", "tools")`, `supports_json=true`, `supports_doctor=true`, `supports_update=false`.

2. Replace the manual `AppMode::Version`, `AppMode::License`, `AppMode::Doctor`, and `AppMode::Completions` output code in `crates/prompter/src/main.rs` with `StandardCommand` or `cli-common` helper calls. Keep the `Run`, `List`, `Tree`, `Validate`, and `Init` execution paths unchanged.

3. Refactor `crates/prompter/src/doctor.rs` so the crate-specific TOML/library checks become a `DoctorChecks` implementation consumed by `cli-common::doctor::run_doctor` instead of maintaining a wholly separate doctor renderer.

4. In `crates/prompter/src/completions.rs`, keep the existing bash/zsh/fish augmentation logic, but build the base completion script through the shared `generate_completions` wrapper first, then post-process it. Do not remove dynamic profile suggestions.

5. Add tests proving:
   - `prompter version --json` still returns JSON
   - `prompter doctor --json` still returns JSON
   - `prompter completions bash` still contains the instruction header and dynamic helper functions
  </action>
  <verify>
    <automated>cargo test -p tftio-prompter</automated>
  </verify>
  <acceptance_criteria>
    - `crates/prompter/src/main.rs` contains `ToolSpec`
    - `crates/prompter/src/main.rs` contains `StandardCommand`
    - `crates/prompter/src/doctor.rs` implements `DoctorChecks`
    - `crates/prompter/src/completions.rs` still contains `__prompter_bash_list_profiles`
    - `cargo test -p tftio-prompter` exits 0
  </acceptance_criteria>
  <done>
    `prompter` keeps its custom profile-aware UX, but the base metadata and doctor/completion scaffolding are no longer bespoke.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add workspace-wide CLI consistency verification and document the boundary of cli-common</name>
  <files>Cargo.toml, CLAUDE.md, .planning/PROJECT.md, justfile</files>
  <read_first>
    - Cargo.toml
    - CLAUDE.md
    - .planning/PROJECT.md
    - justfile
    - crates/*/src/main.rs
    - crates/*/tests/cli*.rs
  </read_first>
  <action>
Add durable enforcement for the new shared UX contract:

1. In `justfile`, add a `cli-consistency` recipe that runs a fixed set of commands covering every binary crate's base UX. Include exact commands for:
   - `cargo run -p tftio-unvenv -- --help`
   - `cargo run -p tftio-gator -- --help`
   - `cargo run -p tftio-todoer -- --help`
   - `cargo run -p tftio-silent-critic -- --help`
   - `cargo run -p tftio-prompter -- --help`
   - `cargo run -p tftio-asana-cli -- --help`
   - `cargo run -p tftio-bsky-comment-extractor -- --help`

2. Extend the recipe with one JSON-mode smoke check for `gator`, `todoer`, and `silent-critic` that asserts the output contains `"ok"` and `"command"`.

3. Update `CLAUDE.md` with a new section that states the invariant explicitly: base CLI UX belongs in `cli-common`; crate-local code must only own domain-specific commands or output.

4. Update `.planning/PROJECT.md` current-state/context text so this phase is discoverable as the active workspace refactor instead of only the prior `bsky-comment-extractor` milestone context.

5. If root `Cargo.toml` still lists crates that only remain because of now-removed duplicated helpers, remove those direct crate dependencies from consumers and keep versions centralized only at the workspace root.
  </action>
  <verify>
    <automated>just cli-consistency && just test && just lint</automated>
  </verify>
  <acceptance_criteria>
    - `justfile` contains `cli-consistency:`
    - `justfile` contains `cargo run -p tftio-unvenv -- --help`
    - `justfile` contains `cargo run -p tftio-prompter -- --help`
    - `CLAUDE.md` contains `base CLI UX belongs in cli-common`
    - `.planning/PROJECT.md` mentions the CLI common unification phase
    - `just cli-consistency` exits 0
  </acceptance_criteria>
  <done>
    The shared UX contract is enforced by workspace tooling and documented so future tool work does not reintroduce local duplication.
  </done>
</task>

</tasks>

<verification>
- `cargo test -p tftio-prompter`
- `just cli-consistency`
- `just test`
- `just lint`
</verification>

<success_criteria>
- `prompter` is no longer a holdout with bespoke base UX wiring
- workspace tooling verifies the shared contract across all binaries
- project docs and instructions explicitly encode the cli-common boundary
</success_criteria>

<output>
After completion, create `.planning/phases/01-i-want-you-to-examine-all-the-code-here-and-extract-into-the-cli-common-create-all-common-functionality-be-extremely-thorough-i-want-these-tools-to-have-a-completely-uniform-base-ui-ux-and-i-want-all-dependencies-to-be-centralized/01-04-SUMMARY.md`
</output>
