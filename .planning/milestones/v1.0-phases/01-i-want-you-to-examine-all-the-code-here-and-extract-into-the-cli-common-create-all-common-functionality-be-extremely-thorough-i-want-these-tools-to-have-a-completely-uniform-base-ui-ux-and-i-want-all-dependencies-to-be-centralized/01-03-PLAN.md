---
phase: 01-cli-common-unification
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - crates/unvenv/src/main.rs
  - crates/bsky-comment-extractor/src/cli.rs
  - crates/bsky-comment-extractor/src/main.rs
  - crates/asana-cli/src/cli/mod.rs
  - crates/asana-cli/src/doctor.rs
  - crates/asana-cli/src/main.rs
  - crates/asana-cli/src/output/mod.rs
autonomous: true
requirements:
  - CLI-UNIFY-03
must_haves:
  truths:
    - "unvenv, bsky-comment-extractor, and asana-cli use the same shared base primitives for metadata commands, text errors, and progress behavior where applicable"
    - "bsky-comment-extractor no longer owns a private spinner constructor"
    - "asana-cli no longer prints debug-formatted errors from main.rs"
  artifacts:
    - path: "crates/unvenv/src/main.rs"
      provides: "shared metadata command wiring"
      contains: "StandardCommand"
    - path: "crates/bsky-comment-extractor/src/main.rs"
      provides: "shared spinner and error handling"
      contains: "make_spinner"
    - path: "crates/asana-cli/src/main.rs"
      provides: "shared top-level error rendering"
      contains: "print_error"
  key_links:
    - from: "crates/bsky-comment-extractor/src/main.rs"
      to: "crates/cli-common/src/progress.rs"
      via: "spinner helper import"
      pattern: "make_spinner"
    - from: "crates/asana-cli/src/cli/mod.rs"
      to: "crates/cli-common/src/command.rs"
      via: "standard metadata commands"
      pattern: "StandardCommand"
---

<objective>
Migrate the user-oriented CLIs to the shared `cli-common` base so text-mode UX, metadata commands, and progress behavior stop diverging across `unvenv`, `bsky-comment-extractor`, and `asana-cli`.

Purpose: Standardize the human-facing binaries after the shared foundation is stable.
Output: Three consumer crates using the shared metadata, error, and progress primitives without losing domain-specific behavior.
</objective>

<execution_context>
@/Users/jfb/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jfb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-i-want-you-to-examine-all-the-code-here-and-extract-into-the-cli-common-create-all-common-functionality-be-extremely-thorough-i-want-these-tools-to-have-a-completely-uniform-base-ui-ux-and-i-want-all-dependencies-to-be-centralized/01-RESEARCH.md
@.planning/phases/01-i-want-you-to-examine-all-the-code-here-and-extract-into-the-cli-common-create-all-common-functionality-be-extremely-thorough-i-want-these-tools-to-have-a-completely-uniform-base-ui-ux-and-i-want-all-dependencies-to-be-centralized/01-01-PLAN.md

<interfaces>
Current user-facing divergence:
- `crates/unvenv/src/main.rs` implements metadata commands inline
- `crates/bsky-comment-extractor/src/main.rs` implements a private spinner and ad-hoc error formatting
- `crates/asana-cli/src/cli/mod.rs` manually wires metadata commands and `main.rs` prints `{:?}` errors
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace unvenv and bsky-comment-extractor entrypoint glue with cli-common primitives</name>
  <files>crates/unvenv/src/main.rs, crates/bsky-comment-extractor/src/cli.rs, crates/bsky-comment-extractor/src/main.rs</files>
  <read_first>
    - crates/unvenv/src/main.rs
    - crates/bsky-comment-extractor/src/cli.rs
    - crates/bsky-comment-extractor/src/main.rs
    - crates/cli-common/src/app.rs
    - crates/cli-common/src/command.rs
    - crates/cli-common/src/error.rs
    - crates/cli-common/src/progress.rs
  </read_first>
  <action>
Apply the shared base UX with these exact changes:

1. In `crates/unvenv/src/main.rs`, replace inline handling for `Version`, `License`, `Completions`, `Doctor`, and `Update` with `ToolSpec` + `StandardCommand` dispatch. Preserve `Scan` as the default operational path and preserve existing exit codes.

2. In `crates/bsky-comment-extractor/src/main.rs`, delete the local `make_spinner(quiet, handle)` function and replace it with `tftio_cli_common::progress::make_spinner(!cli.quiet, &format!("Fetching posts for {}... 0 records", cli.handle))`.

3. In `crates/bsky-comment-extractor/src/main.rs`, replace the top-level error branch `eprintln!("Error: {e:#}")` with `print_error("extract", false, &format!("{e:#}"))`.

4. In `crates/bsky-comment-extractor/src/cli.rs`, add shared metadata subcommands `Version`, `License`, `Completions`, and `Doctor` without breaking the existing primary extraction invocation. The extraction path must still accept `bce alice.bsky.social` unchanged.

5. Add tests proving:
   - `unvenv` still parses and runs `Scan` with no subcommand
   - `bce alice.bsky.social` still parses as extraction mode
   - `bce completions bash` parses successfully
   - `bce license` parses successfully
  </action>
  <verify>
    <automated>cargo test -p tftio-unvenv && cargo test -p tftio-bsky-comment-extractor</automated>
  </verify>
  <acceptance_criteria>
    - `crates/unvenv/src/main.rs` contains `ToolSpec`
    - `crates/unvenv/src/main.rs` contains `StandardCommand`
    - `crates/bsky-comment-extractor/src/main.rs` contains `make_spinner(` imported from `tftio_cli_common`
    - `crates/bsky-comment-extractor/src/main.rs` contains `print_error(`
    - `crates/bsky-comment-extractor/src/main.rs` no longer defines `fn make_spinner`
    - `cargo test -p tftio-unvenv` exits 0
    - `cargo test -p tftio-bsky-comment-extractor` exits 0
  </acceptance_criteria>
  <done>
    The smaller user-facing CLIs now share the same metadata and progress primitives without losing their core invocation syntax.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire asana-cli to the shared base contract while preserving its command tree</name>
  <files>crates/asana-cli/src/cli/mod.rs, crates/asana-cli/src/doctor.rs, crates/asana-cli/src/main.rs, crates/asana-cli/src/output/mod.rs</files>
  <read_first>
    - crates/asana-cli/src/cli/mod.rs
    - crates/asana-cli/src/doctor.rs
    - crates/asana-cli/src/main.rs
    - crates/asana-cli/src/output/mod.rs
    - crates/cli-common/src/app.rs
    - crates/cli-common/src/command.rs
    - crates/cli-common/src/error.rs
  </read_first>
  <action>
Keep the Asana command tree intact, but move base UX wiring to `cli-common`:

1. In `crates/asana-cli/src/cli/mod.rs`, define one `ToolSpec` with exact values `bin_name="asana-cli"`, `display_name="Asana CLI"`, `license=LicenseType::MIT`, `repo=RepoInfo::new("tftio-stuff", "tools")`, `supports_json=false`, `supports_doctor=true`, `supports_update=true`.

2. Route the existing `Version`, `License`, `Completions`, `Doctor`, and `Update` commands through `StandardCommand` instead of hand-written match arms. Preserve `Manpage` as a crate-specific command.

3. In `crates/asana-cli/src/main.rs`, replace `eprintln!("{err:?}")` with `print_error("asana-cli", false, &err.to_string())` and keep the tracing log line.

4. In `crates/asana-cli/src/doctor.rs` and `crates/asana-cli/src/output/mod.rs`, remove any helpers that become redundant because `cli-common` now owns the shared base rendering.

5. Add or update tests to prove `asana-cli version`, `asana-cli license`, `asana-cli completions bash`, and `asana-cli doctor` still route and exit through the same command tree.
  </action>
  <verify>
    <automated>cargo test -p tftio-asana-cli</automated>
  </verify>
  <acceptance_criteria>
    - `crates/asana-cli/src/cli/mod.rs` contains `ToolSpec`
    - `crates/asana-cli/src/cli/mod.rs` contains `StandardCommand`
    - `crates/asana-cli/src/main.rs` contains `print_error(`
    - `crates/asana-cli/src/main.rs` no longer contains `eprintln!("{err:?}")`
    - `cargo test -p tftio-asana-cli` exits 0
  </acceptance_criteria>
  <done>
    `asana-cli` preserves its broad domain command surface while its base metadata and error UX now come from `cli-common`.
  </done>
</task>

</tasks>

<verification>
- `cargo test -p tftio-unvenv`
- `cargo test -p tftio-bsky-comment-extractor`
- `cargo test -p tftio-asana-cli`
- `cargo clippy -p tftio-unvenv -p tftio-bsky-comment-extractor -p tftio-asana-cli -- -D warnings`
</verification>

<success_criteria>
- Human-facing CLIs share the same metadata and error primitives
- `bsky-comment-extractor` no longer owns unique spinner glue
- `asana-cli` loses duplicated base-command wiring without losing specialized behavior
</success_criteria>

<output>
After completion, create `.planning/phases/01-i-want-you-to-examine-all-the-code-here-and-extract-into-the-cli-common-create-all-common-functionality-be-extremely-thorough-i-want-these-tools-to-have-a-completely-uniform-base-ui-ux-and-i-want-all-dependencies-to-be-centralized/01-03-SUMMARY.md`
</output>
