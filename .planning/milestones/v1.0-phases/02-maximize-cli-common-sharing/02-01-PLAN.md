---
phase: 02-maximize-cli-common-sharing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cli-common/Cargo.toml
  - crates/cli-common/src/lib.rs
  - crates/cli-common/src/app.rs
  - crates/cli-common/src/command.rs
  - crates/cli-common/src/completions.rs
  - crates/cli-common/src/doctor.rs
  - crates/cli-common/src/output.rs
  - crates/cli-common/src/error.rs
  - crates/cli-common/src/json.rs
autonomous: true
requirements:
  - CLI-SHARE-01
  - CLI-SHARE-02
must_haves:
  truths:
    - "`cli-common` exposes shared constructors and adapters that remove repeated `tool_spec()` / `NoDoctor` / metadata-command boilerplate from consumers"
    - "`cli-common` can render completion instructions/scripts to memory before printing so augmented consumers do not need to duplicate generation logic"
    - "`cli-common` exposes structured doctor-report helpers reusable for plain-text and JSON doctor output"
  artifacts:
    - path: "crates/cli-common/src/app.rs"
      provides: "Shared tool metadata presets/builders"
      contains: "workspace_tool"
    - path: "crates/cli-common/src/command.rs"
      provides: "Shared metadata-command adapter helpers"
      contains: "run_standard_command"
    - path: "crates/cli-common/src/completions.rs"
      provides: "Completion rendering helpers that return strings/buffers"
      contains: "render_completion"
    - path: "crates/cli-common/src/doctor.rs"
      provides: "Structured doctor reporting"
      contains: "DoctorReport"
  key_links:
    - from: "crates/cli-common/src/completions.rs"
      to: "crates/prompter/src/completions.rs"
      via: "buffer-first completion generation for post-processing"
      pattern: "render_.*completion|generate_completions"
    - from: "crates/cli-common/src/app.rs"
      to: "crates/*/src/main.rs"
      via: "shared tool metadata presets"
      pattern: "workspace_tool|ToolSpec"
---

<objective>
Expand `tftio-cli-common` beyond the Phase 01 contract so it owns the reusable adapter layer that still lives in individual tools.

Purpose: make later consumer migrations delete local boilerplate instead of re-wrapping existing shared primitives.
Output: new `cli-common` constructors, metadata-command adapters, completion rendering APIs, and structured doctor helpers.
</objective>

<execution_context>
@/Users/jfb/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jfb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-maximize-cli-common-sharing/02-RESEARCH.md
@.planning/phases/02-maximize-cli-common-sharing/02-VALIDATION.md

<interfaces>
Repeated consumer patterns to absorb:
- local `tool_spec()` constructors
- local `NoDoctor` adapters
- local `run_meta_command()` mappers
- `prompter` completion instruction/script buffering
- `prompter` parallel doctor plain-text/JSON state shaping
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reusable app and metadata-command adapters to `cli-common`</name>
  <files>crates/cli-common/src/app.rs, crates/cli-common/src/command.rs, crates/cli-common/src/lib.rs</files>
  <read_first>
    - crates/cli-common/src/app.rs
    - crates/cli-common/src/command.rs
    - crates/gator/src/main.rs
    - crates/todoer/src/main.rs
    - crates/silent-critic/src/main.rs
    - crates/bsky-comment-extractor/src/main.rs
    - crates/prompter/src/main.rs
  </read_first>
  <action>
Create the shared adapter surface needed to delete repeated per-tool metadata boilerplate:

1. Add `ToolSpec` constructors/helpers for common workspace cases so most tools no longer need bespoke `tool_spec()` functions.
2. Add a shared doctorless adapter to replace local `NoDoctor` structs.
3. Add metadata-command helper APIs so local `run_meta_command()` functions become trivial wrappers or disappear entirely.
4. Add focused tests proving the helper surface preserves JSON-vs-text version behavior and refuses unsupported doctor/update paths cleanly.
  </action>
  <verify>
    <automated>cargo test -p tftio-cli-common && cargo clippy -p tftio-cli-common -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `cli-common` exports shared constructors/adapters that can replace repeated `tool_spec()` definitions
    - `cli-common` exports a doctorless adapter so consumers no longer need local `NoDoctor` structs
    - `cargo test -p tftio-cli-common` exits 0
    - `cargo clippy -p tftio-cli-common -- -D warnings` exits 0
  </acceptance_criteria>
  <done>
    The reusable metadata/adapter layer exists in `cli-common`, ready for consumer migrations.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add buffer-first completions and structured doctor-report helpers</name>
  <files>crates/cli-common/src/completions.rs, crates/cli-common/src/doctor.rs, crates/cli-common/src/output.rs, crates/cli-common/src/error.rs</files>
  <read_first>
    - crates/cli-common/src/completions.rs
    - crates/cli-common/src/doctor.rs
    - crates/prompter/src/completions.rs
    - crates/prompter/src/doctor.rs
    - crates/unvenv/src/main.rs
    - crates/asana-cli/src/doctor.rs
  </read_first>
  <action>
Add the remaining generally useful primitives that Phase 01 revealed were missing:

1. Extend completions so callers can render instructions and completion scripts into strings/buffers before writing to stdout.
2. Add structured doctor-report helpers that can be rendered as either shared plain text or JSON without tools reimplementing the same state-to-output transformation.
3. Add any reusable output/error presentation helpers needed by later migrations, but do not move domain-specific summaries or tables into `cli-common`.
4. Lock the new behavior down with unit tests before any consumer crate depends on it.
  </action>
  <verify>
    <automated>cargo test -p tftio-cli-common && cargo clippy -p tftio-cli-common -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cli-common/src/completions.rs` can render completion content without writing directly to stdout
    - `crates/cli-common/src/doctor.rs` contains shared structured report helpers reusable by JSON and plain-text doctor flows
    - focused tests cover the new completion and doctor helper surfaces
    - `cargo test -p tftio-cli-common` exits 0
  </acceptance_criteria>
  <done>
    `cli-common` now owns the primitives required to eliminate the remaining duplicated adapter logic in consumers.
  </done>
</task>
