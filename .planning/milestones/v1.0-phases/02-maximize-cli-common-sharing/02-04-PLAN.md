---
phase: 02-maximize-cli-common-sharing
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - Cargo.toml
  - Cargo.lock
  - crates/cli-common/Cargo.toml
  - justfile
  - scripts/test-cli-metadata-consistency.sh
  - tests/cli/lib.sh
  - tests/cli/01-metadata-commands.sh
  - tests/cli/02-json-contracts.sh
  - tests/cli/03-primary-flows.sh
  - tests/cli/04-prompter-doctor.sh
  - CLAUDE.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - CLI-SHARE-04
must_haves:
  truths:
    - "remaining shared dependencies are centralized behind `cli-common` where practical and justified by actual shared behavior"
    - "repository-level tests enforce the absence of the boilerplate patterns extracted in Plans 01-03"
    - "documentation states what still belongs in tool crates versus `cli-common`"
  artifacts:
    - path: "justfile"
      provides: "repository-level enforcement for maximal sharing"
      contains: "cli-metadata-consistency|cli-consistency"
    - path: "tests/cli/"
      provides: "shell suite covering the expanded shared contract"
      contains: "metadata|json|doctor|completion"
    - path: "CLAUDE.md"
      provides: "documented boundary for `cli-common` versus crate-local logic"
      contains: "cli-common"
  key_links:
    - from: "justfile"
      to: "tests/cli/*.sh"
      via: "repository-level shell suite"
      pattern: "cli-metadata-consistency"
    - from: "Cargo.toml"
      to: "crates/cli-common/Cargo.toml"
      via: "dependency centralization"
      pattern: "workspace = true"
---

<objective>
Finish the maximal-sharing milestone by cleaning up remaining shared dependency placement, strengthening repository-level enforcement, and documenting the intentional boundary.

Purpose: keep the extracted shared surface from regressing and make future CLI additions follow the same model automatically.
Output: dependency cleanup, stronger shell/cargo enforcement, and updated documentation for the shared/local split.
</objective>

<execution_context>
@/Users/jfb/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jfb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-maximize-cli-common-sharing/02-RESEARCH.md
@.planning/phases/02-maximize-cli-common-sharing/02-VALIDATION.md
@.planning/phases/02-maximize-cli-common-sharing/02-01-PLAN.md
@.planning/phases/02-maximize-cli-common-sharing/02-02-PLAN.md
@.planning/phases/02-maximize-cli-common-sharing/02-03-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Centralize the remaining shared dependencies and add enforcement for extracted boilerplate</name>
  <files>Cargo.toml, Cargo.lock, crates/cli-common/Cargo.toml, justfile, tests/cli/*.sh</files>
  <read_first>
    - Cargo.toml
    - crates/cli-common/Cargo.toml
    - justfile
    - tests/cli/01-metadata-commands.sh
    - tests/cli/02-json-contracts.sh
    - tests/cli/03-primary-flows.sh
    - tests/cli/04-prompter-doctor.sh
  </read_first>
  <action>
After the earlier plans land, audit direct crate dependencies and enforcement gaps:

1. Move shared dependency usage behind `cli-common` where at least two tools benefit and the abstraction is now real, not speculative.
2. Extend the shell suite and/or `just` recipes to detect the extracted boilerplate patterns staying deleted (for example, repeated local `tool_spec()` / doctorless / metadata-command wrappers where they should no longer exist).
3. Keep enforcement pragmatic: check the actual shared contract, not arbitrary style preferences.
  </action>
  <verify>
    <automated>just cli-metadata-consistency && just cli-consistency</automated>
  </verify>
  <acceptance_criteria>
    - remaining shared dependencies are centralized where practical
    - repository-level shell checks cover the expanded shared contract and deleted boilerplate
    - `just cli-metadata-consistency` and `just cli-consistency` exit 0
  </acceptance_criteria>
  <done>
    The repository now enforces the maximal shared CLI contract with automated checks.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Document the final `cli-common` boundary and run the full verification suite</name>
  <files>CLAUDE.md, .planning/PROJECT.md, justfile</files>
  <read_first>
    - CLAUDE.md
    - .planning/PROJECT.md
    - .planning/phases/02-maximize-cli-common-sharing/02-RESEARCH.md
    - .planning/phases/02-maximize-cli-common-sharing/02-VALIDATION.md
  </read_first>
  <action>
Update the repository guidance to reflect the final extraction boundary:

1. Document what must now live in `cli-common` versus what must remain local to a tool crate.
2. Call out any intentional exceptions discovered during the milestone.
3. Run the full repository verification suite and fix any regressions uncovered by the broader checks.
  </action>
  <verify>
    <automated>just cli-metadata-consistency && just cli-consistency && just test && just lint</automated>
  </verify>
  <acceptance_criteria>
    - repository guidance documents the maximal shared boundary clearly
    - the full automated suite exits 0
    - the phase is ready for verification with minimal remaining ambiguity about what belongs in `cli-common`
  </acceptance_criteria>
  <done>
    The maximal-sharing milestone ends with an enforceable, documented shared boundary and a green repository-level verification suite.
  </done>
</task>
